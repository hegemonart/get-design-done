---
phase: 13.3-plugin-update-checker
plan: 01
subsystem: infra
tags: [schema, json-schema, ajv, config, update-checker]

# Dependency graph
requires:
  - phase: 13-ci-cd
    provides: "reference/schemas/config.schema.json Draft-07 schema + scripts/validate-schemas.cjs ajv-cli CI gate"
provides:
  - "Optional top-level `update_dismissed: string` property in .design/config.json schema"
  - "Storage contract for per-version plugin-update dismissal flag (D-13, D-14, D-21)"
affects:
  - 13.3-02-hook-update-check
  - 13.3-04-check-update-command

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additive schema extension: new optional sibling property, additionalProperties: true preserved, existing keys untouched"

key-files:
  created: []
  modified:
    - reference/schemas/config.schema.json

key-decisions:
  - "[Phase 13.3-01] update_dismissed is an optional top-level string (not required) — users who never dismiss never write it"
  - "[Phase 13.3-01] description string documents both writers (slash command --dismiss and hook --dismiss path) so the schema is self-explanatory to CI reviewers"

patterns-established:
  - "Schema addition pattern: insert new property block after the last existing sibling, keep $id/$schema/title unchanged, verify with ajv-cli fixture test (positive + negative)"

requirements-completed: []

# Metrics
duration: ~2 min
completed: 2026-04-19
---

# Phase 13.3 Plan 01: Schema extension for update_dismissed field — Summary

**Optional `update_dismissed: string` property added to `reference/schemas/config.schema.json`, unblocking the plugin-update-checker's per-version dismissal flag without tripping Phase 13's schema-validation CI gate.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-18T21:52:36Z
- **Completed:** 2026-04-18T21:54:05Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Added `update_dismissed` optional top-level string property to the `.design/config.json` Draft-07 schema, with a `description` documenting both writer paths (`/gdd:check-update --dismiss` and `hooks/update-check.sh` on the `--dismiss` code path).
- Preserved `additionalProperties: true` and all existing keys (`model_profile`, `parallelism`) unchanged — pure additive edit, diff is exactly 4 inserted lines.
- Verified the schema compiles under ajv-cli and that a sample config carrying `update_dismissed: "v1.0.7.3"` validates clean, while an integer-typed `update_dismissed` correctly fails with `must be string`.

## Task Commits

1. **Task 1: Add `update_dismissed` string property to `config.schema.json`** — `d9b5300` (feat)

## Files Created/Modified

- `reference/schemas/config.schema.json` — added optional `update_dismissed: string` property (4 lines inserted)

## Exact JSON Block Added

```json
"update_dismissed": {
  "type": "string",
  "description": "Latest plugin tag (e.g. \"v1.0.7.3\") whose update nudge the user has dismissed. Set by /gdd:check-update --dismiss and by hooks/update-check.sh on the --dismiss code path. When a newer tag ships, the nudge reappears."
}
```

Inserted as a sibling of `parallelism` inside the top-level `properties` object. `$schema`, `$id`, `title`, `type`, `additionalProperties` unchanged.

## Verify-Command Transcript

```text
$ node -e "const s=require('./reference/schemas/config.schema.json'); if(!s.properties.update_dismissed) process.exit(1); if(s.properties.update_dismissed.type!=='string') process.exit(2); console.log('ok');"
ok

$ grep -c '"update_dismissed"' reference/schemas/config.schema.json
1

$ grep -c '"type": "string"' reference/schemas/config.schema.json
2

$ node scripts/validate-schemas.cjs
validate-schemas: results
  [OK] plugin via ajv-validate
  [OK] marketplace via ajv-validate
  [OK] hooks via ajv-validate
  [OK] config via ajv-compile (data not present in repo tree — schema compiled only)
  [OK] intel via ajv-compile (data not present in repo tree — schema compiled only)
summary: 5 pair(s) checked, 0 failure(s)

$ npx --yes ajv-cli@5 validate -s reference/schemas/config.schema.json -d /tmp/gdd-13.3-01-fixtures/valid.json
/tmp/gdd-13.3-01-fixtures/valid.json valid

$ npx --yes ajv-cli@5 validate -s reference/schemas/config.schema.json -d /tmp/gdd-13.3-01-fixtures/invalid-int.json
/tmp/gdd-13.3-01-fixtures/invalid-int.json invalid
[
  {
    instancePath: '/update_dismissed',
    schemaPath: '#/properties/update_dismissed/type',
    keyword: 'type',
    params: { type: 'string' },
    message: 'must be string'
  }
]
# exit=1 — negative fixture correctly rejected

$ npx --yes ajv-cli@5 validate -s reference/schemas/config.schema.json -d /tmp/gdd-13.3-01-fixtures/existing-only.json
/tmp/gdd-13.3-01-fixtures/existing-only.json valid
# regression: existing model_profile + parallelism without update_dismissed still valid
```

All four must-have truths confirmed:

1. Schema accepts an optional top-level `update_dismissed: string` — compiled and validated under ajv.
2. `{"update_dismissed": "v1.0.7.3"}` passes `scripts/validate-schemas.cjs` with exit 0.
3. Integer `update_dismissed` is rejected by ajv (`must be string`), exit 1.
4. Existing `model_profile` and `parallelism` keys still validate unchanged.

## Decisions Made

None — executed plan as specified. D-21 (schema addition) and the storage contract for D-13/D-14 are now locked in via this schema change.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

Local environment does not have `ajv` installed as a direct dependency (tried `node -e "require('ajv')"` initially). Plan anticipated this and specified the `scripts/validate-schemas.cjs` fallback, which passed cleanly. Additionally ran the ajv-cli-based fixture tests via `npx --yes ajv-cli@5` (the same mechanism CI uses) — all three fixtures (positive, negative, regression) behaved as expected. No workaround needed beyond the plan's pre-specified fallback path.

## Known Stubs

None — schema addition is fully wired for downstream plans 13.3-02 and 13.3-04.

## Threat Flags

None — this plan is the stated mitigation for the plan's own `<threat_model>`. T-13.3.01-02 (CI denial-of-service via malformed JSON) was mitigated by the `node -e` parse check and the `validate-schemas.cjs` ajv-compile pass before commit; T-13.3.01-01 (tampering) is accepted per branch-protection/code-review.

## User Setup Required

None — schema-only change, no external service configuration.

## Next Phase Readiness

- **13.3-02 (hook writes dismissal flag):** Unblocked — config.json produced by the hook's `--dismiss` code path will pass the Phase 13 ajv-cli CI gate.
- **13.3-04 (`/gdd:check-update --dismiss` slash command):** Unblocked — same rationale.
- **Phase 13 CI:** Green; `validate-schemas.cjs` reports 5/5 pairs passing locally; ajv-cli on the real data files (plugin, marketplace, hooks) still passes with the modified schema in place.

## Self-Check

Running the self-check items specified in the workflow:

```text
$ [ -f reference/schemas/config.schema.json ] && echo "FOUND: reference/schemas/config.schema.json" || echo "MISSING"
FOUND: reference/schemas/config.schema.json

$ git log --oneline --all | grep -q "d9b5300" && echo "FOUND: d9b5300" || echo "MISSING"
FOUND: d9b5300
```

## Self-Check: PASSED

---
*Phase: 13.3-plugin-update-checker*
*Completed: 2026-04-19*
