---
phase: 13.3-plugin-update-checker
plan: 06
title: "Version bump + CHANGELOG + Phase 13.3 baseline lock → v1.0.7.3"
type: execute
wave: 3
depends_on: ["13.3-01", "13.3-02", "13.3-03", "13.3-04", "13.3-05"]
requirements: []
files_modified:
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - CHANGELOG.md
  - test-fixture/baselines/phase-13.3/BASELINE.md
  - test-fixture/baselines/phase-13.3/README.md
autonomous: true
tags: [release, version-bump, changelog, baseline]
must_haves:
  truths:
    - ".claude-plugin/plugin.json `version` is `1.0.7.3` (not `1.0.7`, not `v1.0.7.3`)"
    - ".claude-plugin/marketplace.json both version fields (metadata.version and plugins[0].version) are `1.0.7.3`"
    - "CHANGELOG.md has a new top section `## [1.0.7.3] — <date>` above the `## [1.0.7]` section"
    - "The `## [1.0.7.3]` section lists: new hook (update-check.sh), new agent (design-update-checker), new skill (/gdd:check-update), schema addition (update_dismissed), 6 safe-window surfaces modified"
    - "test-fixture/baselines/phase-13.3/ exists with BASELINE.md and README.md following the phase-13 baseline template"
    - "scripts/release-smoke-test.cjs still exits 0 against the new baseline (structural lock only — this phase adds no user-visible pipeline output)"
    - "Phase 13's auto-tag + release workflow will pick up the version change on merge-to-main and auto-create v1.0.7.3 tag (no new CI plumbing required)"
    - "README.md relock snippet uses explicit REPO_ROOT capture (REPO_ROOT=\"$(pwd)\") — does NOT rely on $OLDPWD, which is unset in most invocation contexts"
  artifacts:
    - path: .claude-plugin/plugin.json
      provides: "Canonical version source — 1.0.7.3"
      contains: "1.0.7.3"
    - path: .claude-plugin/marketplace.json
      provides: "Marketplace-facing version source (dual field)"
      contains: "1.0.7.3"
    - path: CHANGELOG.md
      provides: "## [1.0.7.3] section above [1.0.7]"
      contains: "## [1.0.7.3]"
    - path: test-fixture/baselines/phase-13.3/BASELINE.md
      provides: "Phase 13.3 regression baseline manifest"
      min_lines: 20
    - path: test-fixture/baselines/phase-13.3/README.md
      provides: "Phase 13.3 baseline relock instructions"
      min_lines: 15
  key_links:
    - from: .claude-plugin/plugin.json
      to: .github/workflows/release.yml
      via: "Phase 13's auto-tag workflow detects version change and tags v1.0.7.3"
      pattern: '"version"'
    - from: CHANGELOG.md
      to: scripts/extract-changelog-section.cjs
      via: "Release workflow extracts the v1.0.7.3 section for the GitHub Release body"
      pattern: "## [1.0.7.3]"
---

<objective>
Close Phase 13.3 by shipping the version bump, CHANGELOG entry, and a regression baseline lock. Phase 13's existing release automation takes over from there — a push to main with the new version in `plugin.json` auto-tags `v1.0.7.3`, creates the GitHub Release (body extracted from the new CHANGELOG section), and runs the release-smoke-test.

Purpose: D-19 (version bump to 1.0.7.3 in both plugin.json + marketplace.json + CHANGELOG section), D-20 (single-phase baseline relock using Phase 13 pattern; no new CI plumbing).
Output: 3 bumped manifest/changelog files + 1 new baseline directory with 2 small files.

**Cross-branch awareness (noted in planning context, not a task):** If Phase 13.2 merges first and ships as v1.0.7.2, rebase this plan's version-bump step so plugin.json moves from 1.0.7.2 → 1.0.7.3 instead of 1.0.7 → 1.0.7.3. The CHANGELOG section ordering remains ## [1.0.7.3] on top.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.3-plugin-update-checker/13.3-CONTEXT.md
@.claude-plugin/plugin.json
@.claude-plugin/marketplace.json
@CHANGELOG.md
@test-fixture/baselines/phase-13/README.md
@test-fixture/baselines/phase-13/BASELINE.md

<interfaces>
Current version state (pre-rebase assumption — phase 13.2 not yet landed):
- .claude-plugin/plugin.json: `"version": "1.0.7"`
- .claude-plugin/marketplace.json: `"metadata": { "version": "1.0.7" }` AND `"plugins": [{ "version": "1.0.7" }]` (two fields, same value)
- CHANGELOG.md: top section is `## [1.0.7] — 2026-04-18`

Phase 13 release automation (from `.github/workflows/release.yml`, locked at v1.0.7):
- Trigger: push to main
- Detect: plugin.json version differs from previous tag
- Action: create annotated git tag `v<version>`, run `scripts/extract-changelog-section.cjs` to pull `## [<version>]` section as release body, use softprops/action-gh-release@v2 to publish the release, run release-smoke-test against the matching `test-fixture/baselines/phase-<N>/` directory.

Phase 13 baseline pattern (reference: test-fixture/baselines/phase-13/):
- `BASELINE.md` — metadata (locked date, plugin version, phase, what-this-baseline-locks, what-changed-vs-previous)
- `README.md` — relock instructions
- Optional `intel/*.json` — output of `scripts/build-intel.cjs` on test-fixture/src; may be empty for CSS-only fixtures
- Phase 13.3 does not change pipeline output, so the baseline is manifest-only (same as phase-13).

scripts/extract-changelog-section.cjs contract: reads CHANGELOG.md, finds the section matching the `plugin.json` version, emits it as a single markdown block. The new `## [1.0.7.3]` section MUST parse cleanly under Keep a Changelog format (Added / Changed / Fixed / Removed subsections as applicable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version in plugin.json + marketplace.json to 1.0.7.3</name>
  <read_first>
    - .claude-plugin/plugin.json (the file being edited)
    - .claude-plugin/marketplace.json (the file being edited — dual version fields)
    - .github/workflows/release.yml (for context — verify it reads plugin.json `.version` and uses it as the tag source)
  </read_first>
  <action>
    Two small edits, executed in sequence.

    **Edit 1 — `.claude-plugin/plugin.json`:**

    Change the single `"version"` field. Current line:

    ```json
      "version": "1.0.7",
    ```

    New line:

    ```json
      "version": "1.0.7.3",
    ```

    No other fields change. Do NOT modify the description, keywords, or anything else — the feature keywords (e.g. "update-checker") can be added in a separate follow-up if needed; this plan's scope is the version bump alone per D-19.

    **Edit 2 — `.claude-plugin/marketplace.json`:**

    TWO fields must change:

    1. Top-level `metadata.version`:

    ```json
      "metadata": {
        ...
        "version": "1.0.7"      →     "version": "1.0.7.3"
      },
    ```

    2. `plugins[0].version` (the single plugin entry):

    ```json
      "plugins": [
        {
          ...
          "version": "1.0.7",    →    "version": "1.0.7.3",
          ...
        }
      ]
    ```

    Both fields must match to keep the Phase 13 CI validation (`plugin.json.version === marketplace.json.metadata.version === marketplace.json.plugins[0].version`) green.

    Implements D-19 (version bump to 1.0.7.3). Phase 13's release.yml workflow will auto-detect this version change on push-to-main and create the `v1.0.7.3` tag + GitHub Release.
  </action>
  <verify>
    <automated>node -e "
    const p=require('./.claude-plugin/plugin.json');
    const m=require('./.claude-plugin/marketplace.json');
    if(p.version!=='1.0.7.3'){console.error('plugin.json not 1.0.7.3: '+p.version);process.exit(1)}
    if(m.metadata.version!=='1.0.7.3'){console.error('marketplace.json metadata.version not 1.0.7.3: '+m.metadata.version);process.exit(2)}
    if(!m.plugins||!m.plugins[0]||m.plugins[0].version!=='1.0.7.3'){console.error('marketplace.json plugins[0].version not 1.0.7.3');process.exit(3)}
    console.log('ok');
    "</automated>
  </verify>
  <acceptance_criteria>
    - `jq -r .version .claude-plugin/plugin.json` returns `1.0.7.3` (or, if jq absent, the above node script exits 0)
    - `jq -r .metadata.version .claude-plugin/marketplace.json` returns `1.0.7.3`
    - `jq -r '.plugins[0].version' .claude-plugin/marketplace.json` returns `1.0.7.3`
    - `git diff .claude-plugin/` shows only the version lines changed (no collateral edits to description/keywords)
    - Phase 12 test `tests/schema-drift.test.cjs` passes (version consistency across plugin.json + marketplace.json)
  </acceptance_criteria>
  <done>
    Both manifests carry version `1.0.7.3` in all three required fields; no other metadata modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ## [1.0.7.3] section to CHANGELOG.md</name>
  <read_first>
    - CHANGELOG.md (the file being edited — existing top section is ## [1.0.7])
    - scripts/extract-changelog-section.cjs (to confirm section-parsing format: `## [<version>] — <date>` with subsections like `### Added — Phase <N>`)
  </read_first>
  <action>
    Insert a new section at the top of CHANGELOG.md, above the current `## [1.0.7]` section and below the `---` separator at line 5. Use today's date (2026-04-19) unless the git system clock disagrees — in that case use the current UTC date.

    New section content:

    ```markdown
    ## [1.0.7.3] — 2026-04-19

    ### Added — Phase 13.3: Plugin Update Checker
    - `hooks/update-check.sh` — SessionStart Bash hook; 24h-cached unauthenticated `GET /repos/hegemonart/get-design-done/releases/latest`; classifies semver delta (major / minor / patch / off-cadence); respects `.design/STATE.md` stage guard (suppresses nudge during `plan`, `design`, `verify`); respects per-version dismissal from `.design/config.json`; silent-on-failure by policy (exit 0 on every error path).
    - `hooks/hooks.json` — registers `update-check.sh` as a second SessionStart command alongside `bootstrap.sh` (run order: bootstrap → update-check).
    - `agents/design-update-checker.md` — Haiku-tier enrichment agent (Phase 10.1 cost governance). Invoked only by `/gdd:check-update --prompt` to produce a 3–5-line "what this release changes for you" summary from the cached release body. Reads-only, inline-text-only output, ends with `## UPDATE-CHECKER COMPLETE`.
    - `skills/check-update/SKILL.md` — `/gdd:check-update` manual slash command. Flags: `--refresh` (bypass 24h TTL, re-fetch now), `--dismiss` (write `update_dismissed: "<tag>"` atomically to `.design/config.json` and delete the rendered banner), `--prompt` (spawn the enrichment agent). Default (no flag) prints cached state.
    - `.design/update-cache.json` — per-user-project runtime cache (written by the hook). Shape: `{checked_at, current_tag, latest_tag, delta, is_newer, changelog_excerpt}`; 500-char excerpt from release body.
    - `.design/update-available.md` — per-user-project runtime rendered banner. Written only when all four gates pass (cache is_newer=true AND stage ∉ {plan,design,verify} AND not dismissed AND latest_tag parsed successfully). Consumed by safe-window skills via `[ -f .design/update-available.md ] && cat .design/update-available.md`.
    - `reference/schemas/config.schema.json` — adds optional `update_dismissed: string` property.
    - Safe-window surfaces: `/gdd:progress`, `/gdd:health`, `/gdd:help`, post-closeout of `/gdd:ship`, `/gdd:complete-cycle`, `/gdd:audit` — each appends the one-line banner-cat tail before its completion marker.
    - `skills/audit/SKILL.md` — `tools:` list extended with `Bash` to enable the banner-cat tail (previously: `Read, Write, Task, Glob`).
    - `test-fixture/baselines/phase-13.3/` — regression baseline lock for v1.0.7.3 (manifest-only, following the phase-13 pattern — this phase adds no pipeline output).

    ### Changed
    - Plugin version: `1.0.7` → `1.0.7.3` (off-cadence patch — does not shift Phase 14 → v1.0.8).

    ### Design principles (Phase 13.3)
    - Never auto-updates. The checker only surfaces a nudge; `/gdd:update` remains the explicit user action.
    - Never interrupts critical work. State-machine guard suppresses the nudge during mid-pipeline stages (`plan`, `design`, `verify`); banner renders only in the 6 documented safe windows.
    - Silent-on-failure. Network timeout, malformed JSON, missing plugin.json, unwritable `.design/` — every path exits 0 without printing to stderr during normal SessionStart.
    - No telemetry. Unauthenticated GitHub Releases fetch; no phone-home, no tracking, no tokens in code.

    ---
    ```

    Keep the existing `## [1.0.7] — 2026-04-18` section intact immediately below.

    Implements D-19 (CHANGELOG entry) — the section becomes the release body on tag creation via Phase 13's `scripts/extract-changelog-section.cjs`.
  </action>
  <verify>
    <automated>node -e "
    const fs=require('fs');
    const c=fs.readFileSync('CHANGELOG.md','utf8');
    if(!/## \[1\.0\.7\.3\]/.test(c)){console.error('no 1.0.7.3 section');process.exit(1)}
    const idx1073=c.indexOf('## [1.0.7.3]');
    const idx107=c.indexOf('## [1.0.7]');
    if(idx1073<0||idx107<0||idx1073>=idx107){console.error('1.0.7.3 must come BEFORE 1.0.7; idx1073='+idx1073+' idx107='+idx107);process.exit(2)}
    if(!/### Added — Phase 13\.3/.test(c)){console.error('missing Added subsection');process.exit(3)}
    console.log('ok');
    " && node scripts/extract-changelog-section.cjs 1.0.7.3 2>&1 | grep -q 'Plugin Update Checker'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## \[1\.0\.7\.3\]' CHANGELOG.md` returns 1
    - `## [1.0.7.3]` appears BEFORE `## [1.0.7]` in the file (byte-offset check above)
    - The `## [1.0.7.3]` section contains subsections `### Added` and `### Changed`
    - Section documents the 6 new/modified surfaces + hook + agent + skill + schema addition
    - `node scripts/extract-changelog-section.cjs 1.0.7.3` returns the full new section without errors (Phase 13 release automation input contract)
  </acceptance_criteria>
  <done>
    CHANGELOG.md has a well-formed `## [1.0.7.3]` section at the top, parsable by scripts/extract-changelog-section.cjs, listing every 13.3 deliverable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create test-fixture/baselines/phase-13.3/ directory with BASELINE.md + README.md</name>
  <read_first>
    - test-fixture/baselines/phase-13/BASELINE.md (reference template — phase 13's manifest-only lock)
    - test-fixture/baselines/phase-13/README.md (reference template — relock instructions)
    - scripts/release-smoke-test.cjs (to confirm which `--baseline` path the release workflow diffs against)
  </read_first>
  <action>
    Create the directory and two files.

    **Directory:** `mkdir -p test-fixture/baselines/phase-13.3`

    **File 1 — `test-fixture/baselines/phase-13.3/BASELINE.md`:**

    ```markdown
    # Phase 13.3 Regression Baseline

    **Locked:** 2026-04-19
    **Plugin version:** 1.0.7.3
    **Phase:** 13.3 — Plugin Update Checker

    ## What this baseline locks

    The deterministic output of `/gdd:explore` on `test-fixture/src/` as of
    plugin v1.0.7.3. The release-time smoke test diffs against this baseline
    on every tag creation; a diff fails the build and surfaces the manual
    rollback command.

    ## Artifacts

    - `intel/*.json` — output of `scripts/build-intel.cjs` on `test-fixture/src/`
      (if the script produces artifacts for this fixture; may be empty for a
      CSS-only / small fixture — see phase-13 baseline for prior shape)
    - `BASELINE.md` — this manifest
    - `README.md` — relock instructions

    At lock time the fixture (`test-fixture/src/App.jsx`, `App.css`, `index.css`)
    is small enough that `build-intel.cjs` produces no deterministic intel slices
    — the baseline is therefore a manifest-only lock, matching the phase-13
    pattern. The release-smoke-test script (`scripts/release-smoke-test.cjs`)
    treats missing artifacts as informational, not failures; only byte-level
    diffs fail the build.

    ## What changed vs phase-13 baseline

    Phase 13.3 does NOT change pipeline output — it only adds:
    - a SessionStart hook (`hooks/update-check.sh`)
    - a Haiku-tier enrichment agent (`agents/design-update-checker.md`)
    - a manual slash command (`skills/check-update/SKILL.md`)
    - a schema field (`update_dismissed`)
    - footer banners on 6 safe-window skills (no change to their core output)

    None of these touch the pipeline stages (brief → explore → plan → design
    → verify) or the intel-build script. The pipeline-output baseline is
    therefore equivalent to phase-13's modulo the plugin version string
    (1.0.7 → 1.0.7.3). Any unexpected diff vs phase-13 in this baseline
    reflects a bug, not intentional change.

    ## Relock procedure

    See `README.md` in this directory, or `CONTRIBUTING.md` §"Baseline
    relock how-to" for the one-shot command.
    ```

    **File 2 — `test-fixture/baselines/phase-13.3/README.md`:**

    This file documents the relock procedure. The relock snippet MUST NOT rely
    on `$OLDPWD` — that variable is only set when the current shell has just
    executed a `cd`, which is NOT the case for most invocations (fresh shell,
    direct paste from README, CI runner, subshell). Instead, capture the repo
    root explicitly at the top of the snippet and reference it from inside the
    subshell.

    ```markdown
    # Phase 13.3 Baseline — Relock Instructions

    To relock this baseline (only if intentional behavior change), run from
    the repo root:

    ```bash
    # Capture the repo root explicitly — do NOT rely on $OLDPWD, which is only
    # set when the shell has just executed a `cd` and is unset in most fresh
    # invocations (CI runners, direct README paste, subshells).
    REPO_ROOT="$(pwd)"
    TMPDIR="$(mktemp -d)"

    # Clone a clean copy of the repo into TMPDIR, then run build-intel.cjs against
    # the test-fixture in the clone. Using the clone avoids touching the working
    # tree's .design/ state.
    (
      cd "$TMPDIR" \
        && git clone "$REPO_ROOT" . \
        && node "$REPO_ROOT/scripts/build-intel.cjs" .
    )

    rm -rf test-fixture/baselines/phase-13.3/intel/
    if [ -d "$TMPDIR/.design/intel" ]; then
      mkdir -p test-fixture/baselines/phase-13.3/intel
      cp "$TMPDIR/.design/intel/"*.json test-fixture/baselines/phase-13.3/intel/
    fi

    rm -rf "$TMPDIR"
    ```

    Then update the "What changed" section of `BASELINE.md` with a short note,
    update `scripts/release-smoke-test.cjs` if the artifact shape changed, and
    commit as part of the phase-13.3 closeout PR.

    The release-smoke-test (`scripts/release-smoke-test.cjs --baseline
    test-fixture/baselines/phase-13.3`) must exit 0 against the freshly-locked
    baseline. CI's release workflow runs this diff on every tag creation.
    ```

    Implements D-20 (single-phase baseline relock using the Phase 13 pattern; manifest-only; no new CI plumbing).
  </action>
  <verify>
    <automated>test -d test-fixture/baselines/phase-13.3 &amp;&amp; test -f test-fixture/baselines/phase-13.3/BASELINE.md &amp;&amp; test -f test-fixture/baselines/phase-13.3/README.md &amp;&amp; grep -q '1.0.7.3' test-fixture/baselines/phase-13.3/BASELINE.md &amp;&amp; grep -q 'Plugin Update Checker' test-fixture/baselines/phase-13.3/BASELINE.md &amp;&amp; grep -q 'mktemp -d' test-fixture/baselines/phase-13.3/README.md &amp;&amp; grep -q 'REPO_ROOT=.*pwd' test-fixture/baselines/phase-13.3/README.md &amp;&amp; ! grep -q '\$OLDPWD' test-fixture/baselines/phase-13.3/README.md &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `test-fixture/baselines/phase-13.3/` directory exists
    - `test-fixture/baselines/phase-13.3/BASELINE.md` exists, references plugin version 1.0.7.3 and Phase 13.3
    - `test-fixture/baselines/phase-13.3/README.md` exists and includes the `mktemp -d` + `build-intel.cjs` relock snippet
    - **Explicit `REPO_ROOT` capture present (checker warning fix):** `grep -E 'REPO_ROOT="\$\(pwd\)"' test-fixture/baselines/phase-13.3/README.md` matches.
    - **No `$OLDPWD` dependency:** `! grep -q '\$OLDPWD' test-fixture/baselines/phase-13.3/README.md` — the snippet must not reference `$OLDPWD`, which is unset in fresh shells / CI / subshells.
    - **Relock snippet is executable as-is** (manual smoke-check before committing): copy the snippet from README.md into a fresh shell from the repo root; confirm it does not error with `OLDPWD: unbound variable` or `git clone: path is not a directory`.
    - `node scripts/release-smoke-test.cjs --baseline test-fixture/baselines/phase-13.3` exits 0 (if the smoke-test supports an explicit --baseline flag; otherwise confirm by running the default path which auto-detects the highest-numbered phase-* directory)
    - `node tests/regression-baseline-drift.test.cjs` exits 0 (the new phase-13.3 directory follows the expected shape — BASELINE.md + README.md + optional intel/)
  </acceptance_criteria>
  <done>
    phase-13.3 baseline directory exists with a manifest-only lock matching the phase-13 template, and the release-smoke-test diffs cleanly against it. Relock snippet uses explicit `REPO_ROOT="$(pwd)"` capture — no `$OLDPWD` dependency.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local commit → release workflow → public tag + GitHub Release | Any version bump here auto-publishes a release on push-to-main |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.3.06-01 | Tampering (wrong version published) | plugin.json + marketplace.json + CHANGELOG | mitigate | Three-way consistency check in Task 1's verify block; Phase 12's schema-drift test blocks publish if any of the three diverge |
| T-13.3.06-02 | Repudiation (missing/bad CHANGELOG body leaks to GitHub Release) | `scripts/extract-changelog-section.cjs` | mitigate | Task 2 verify runs the extractor and greps for "Plugin Update Checker" — if the section is malformed the grep fails before commit |
| T-13.3.06-03 | Availability (release-smoke-test fails against missing baseline) | `test-fixture/baselines/phase-13.3/` | mitigate | Task 3 creates the directory + manifest before merge; Phase 13's smoke-test treats missing `intel/*.json` as informational per the documented phase-13 pattern |
| T-13.3.06-04 | Premature publication (merge ordering vs Phase 13.2) | all three files | mitigate | If 13.2 merges first and ships 1.0.7.2, the executor rebases this plan's version-bump step from 1.0.7 → 1.0.7.3 to 1.0.7.2 → 1.0.7.3 (noted in plan-level `<objective>` cross-branch section); CHANGELOG preserves section ordering |
| T-13.3.06-05 | Availability (documented relock snippet fails in fresh shell) | `README.md` relock block | mitigate | Snippet captures `REPO_ROOT="$(pwd)"` explicitly; acceptance criterion asserts absence of `$OLDPWD` reference; manual smoke-check required pre-commit |
</threat_model>

<verification>
- `node -e "const p=require('./.claude-plugin/plugin.json'); console.log(p.version)"` prints `1.0.7.3`
- `node scripts/extract-changelog-section.cjs 1.0.7.3` returns the new section body
- `node scripts/release-smoke-test.cjs` exits 0 (whatever `--baseline` path the script auto-selects)
- `node tests/schema-drift.test.cjs` exits 0 (version consistency across all manifests)
- `node tests/regression-baseline-drift.test.cjs` exits 0 (baseline directory shape preserved)
- `grep -q 'REPO_ROOT=.*pwd' test-fixture/baselines/phase-13.3/README.md && ! grep -q '\$OLDPWD' test-fixture/baselines/phase-13.3/README.md` (relock snippet uses explicit REPO_ROOT, no $OLDPWD)
- After merge to main, Phase 13's `release.yml` auto-tags `v1.0.7.3` — NOT verified in this plan; confirmed post-merge via `gh release view v1.0.7.3`.
</verification>

<success_criteria>
- Both manifests carry version `1.0.7.3` in all required fields
- CHANGELOG.md has a Keep-a-Changelog-compliant `## [1.0.7.3]` section listing every 13.3 deliverable
- test-fixture/baselines/phase-13.3/ exists with BASELINE.md + README.md following the phase-13 template
- README.md relock snippet uses explicit `REPO_ROOT="$(pwd)"` capture (no `$OLDPWD` dependency)
- Phase 12 + Phase 13 regression tests remain green
- On merge-to-main, Phase 13's release automation will auto-publish v1.0.7.3 without new CI plumbing
</success_criteria>

<output>
After completion, create `.planning/phases/13.3-plugin-update-checker/13.3-06-SUMMARY.md` summarizing: before/after `version` values in both manifests, the exact CHANGELOG section text committed, the baseline directory tree, the relock-snippet smoke-check result (fresh-shell test), and a note that the v1.0.7.3 tag will be created automatically by `release.yml` on merge-to-main.
</output>
