---
phase: 13.3-plugin-update-checker
plan: 03
title: "design-update-checker agent (Haiku enrichment cold path)"
type: execute
wave: 2
depends_on: ["13.3-02"]
requirements: []
files_modified:
  - agents/design-update-checker.md
autonomous: true
tags: [agent, haiku, enrichment, update-checker]
must_haves:
  truths:
    - "agents/design-update-checker.md exists and is a valid plugin agent (frontmatter parses, passes tests/agent-frontmatter.test.cjs)"
    - "Agent is Haiku-tiered (default-tier: haiku) per Phase 10.1 cost governance"
    - "Agent is reads-only (writes: []) — its output is inline text that the spawning /gdd:check-update surfaces; it does NOT write .design/update-available.md"
    - "Agent size fits the Phase 12 agent-size-budget M tier (≤250 lines) — it is a thin summarizer, not a full reasoner"
    - "Agent required_reading lists .design/update-cache.json + the release body (passed in prompt) — nothing else"
  artifacts:
    - path: agents/design-update-checker.md
      provides: "Thin Haiku enrichment agent for /gdd:check-update --prompt"
      contains: "design-update-checker"
      min_lines: 60
  key_links:
    - from: skills/check-update/SKILL.md
      to: agents/design-update-checker.md
      via: "Task() spawn on --prompt flag (wired in plan 13.3-04)"
      pattern: "design-update-checker"
    - from: agents/design-update-checker.md
      to: .design/update-cache.json
      via: "required_reading — consumes the cache written by hooks/update-check.sh"
      pattern: "update-cache.json"
---

<objective>
Ship the cold-path enrichment agent invoked only by `/gdd:check-update --prompt` to produce a richer LLM-generated summary of what changed in a release, complementing (not replacing) the terse hot-path banner.

Purpose: D-01 (two-tier split — hot-path script for default SessionStart, cold-path agent for on-demand richer output), D-18 (Haiku tier — this is a summarizer/classifier, not a reasoner).
Output: one agent file, Haiku-pinned, reads-only, inline-text-only output, ~80–150 lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.3-plugin-update-checker/13.3-CONTEXT.md
@.planning/phases/13.3-plugin-update-checker/13.3-02-PLAN.md
@agents/design-advisor.md
@agents/README.md

<interfaces>
Established agent frontmatter contract (reference: agents/design-advisor.md — another inline-text-only thin agent):

```yaml
---
name: <agent-name>
description: <one-sentence purpose + invocation surface>
tools: Read, Grep, Glob
color: <terminal color>
model: haiku              # soft pin
default-tier: haiku       # hard tier per Phase 10.1
tier-rationale: "<why this tier>"
parallel-safe: always
typical-duration-seconds: 10
reads-only: true
writes: []
---
```

Body convention (Phase 10.1 D-17 — cache-aligned body ordering):
- First non-blank line MUST be `@reference/shared-preamble.md`
- Then heading `# <agent-name>`
- Then standard sections: Role / Required Reading / Inputs / Output / Do Not

Cache shape this agent reads (from hooks/update-check.sh output in plan 13.3-02):
```json
{
  "checked_at": 1713500000,
  "current_tag": "v1.0.7",
  "latest_tag": "v1.0.7.3",
  "delta": "off-cadence",
  "is_newer": true,
  "changelog_excerpt": "…up to 500 chars of release body…"
}
```

The agent also receives a raw release body snippet passed in-prompt by the spawning skill (plan 13.3-04 wires this via the Task() `context.release_body` field).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agents/design-update-checker.md</name>
  <read_first>
    - agents/design-advisor.md (reference: inline-text-only thin agent with Haiku-class scope)
    - agents/README.md (agent authoring contract — frontmatter fields, completion markers, required_reading section)
    - agents/design-assumptions-analyzer.md (reference: another short Haiku-candidate summarizer — structure only; do not mimic content)
  </read_first>
  <action>
    Create `agents/design-update-checker.md` with this exact content (fill minor prose if needed but preserve frontmatter + section headings verbatim):

    ```markdown
    ---
    name: design-update-checker
    description: Cold-path enrichment agent for /gdd:check-update --prompt. Reads .design/update-cache.json plus a release body supplied in the prompt, classifies the delta (major|minor|patch|off-cadence), and returns a 3-5-line human-friendly "what this release changes for you" summary. Does not write any file. Haiku-tier summarizer per Phase 10.1 D-14/D-18.
    tools: Read, Grep, Glob
    color: yellow
    model: haiku
    default-tier: haiku
    tier-rationale: "Pure summarization + classification over a bounded 500-char input; Haiku is correct tier per Phase 10.1 D-14 table (Haiku = verifiers/checkers/summarizers)"
    parallel-safe: always
    typical-duration-seconds: 8
    reads-only: true
    writes: []
    ---

    @reference/shared-preamble.md

    # design-update-checker

    ## Role

    You are the `design-update-checker` agent. `/gdd:check-update --prompt` spawns you AFTER the hot-path SessionStart hook has already classified the semver delta and written `.design/update-cache.json`. Your job is to turn the raw release body into a short, user-facing narrative: "here are the 2-3 things this release changes for *you* (i.e. someone running get-design-done on a real project)."

    You have zero session memory. One invocation = one release summarized. Everything you need is in the prompt plus the files listed in `<required_reading>`.

    **Return inline text only — do NOT write any file.** The spawning skill captures your response and displays it inline. Mirrors the `design-advisor` / `design-plan-checker` contract.

    ## Required Reading

    Before producing output you MUST read:

    1. `.design/update-cache.json` — canonical delta classification, current_tag, latest_tag, changelog_excerpt (up to 500 chars of the release body).
    2. Any `release_body` string supplied in the spawning prompt context (may be fuller than the 500-char excerpt).

    If `.design/update-cache.json` does not exist, return exactly:

    > No update cache found. Run `/gdd:check-update --refresh` first.

    ## Inputs

    From the spawning prompt (`/gdd:check-update --prompt` in plan 13.3-04) you receive:
    - `current_tag` — the installed plugin version (string, e.g. `v1.0.7`)
    - `latest_tag` — the latest released tag (string, e.g. `v1.0.7.3`)
    - `delta` — one of `major` | `minor` | `patch` | `off-cadence`
    - `release_body` — raw release-notes markdown, up to a few thousand chars

    ## Output contract

    Return inline text shaped exactly like this (3–5 lines of prose, no fences, no YAML, no headings):

    ```
    Update: {current_tag} → {latest_tag} ({delta})

    What changed for you:
    - {bullet 1 — concrete user impact}
    - {bullet 2 — concrete user impact}
    - {bullet 3 — optional}

    Run `/gdd:update` to install, or `/gdd:check-update --dismiss` to hide this nudge until the next release.
    ```

    Bullets must name concrete user impact (new command, changed behavior, fixed bug, removed feature), not internal architecture. Cap at 3 bullets. Do not speculate beyond the release body.

    ## Classification boundary

    Do NOT reclassify the delta. The hot-path script already wrote `delta` into `.design/update-cache.json` per D-10. Echo the incoming delta; if it looks wrong, call it out in an inline note but do not override.

    ## Do Not

    - Do not read `.design/config.json` — dismissal state is for the skill/hook, not you.
    - Do not fetch from GitHub — the cache is the source of truth. (You have no WebFetch tool.)
    - Do not write `.design/update-available.md` — that is the hot-path hook's responsibility.
    - Do not reformat the release body as-is. Your job is to compress + reframe.

    ## Completion marker

    Last line of your response MUST be:

    ```
    ## UPDATE-CHECKER COMPLETE
    ```
    ```

    Implements D-01 (cold path), D-18 (Haiku tier). Keeps the agent aligned with `design-advisor`'s inline-text-only pattern (Phase 5 decision) and the Phase 10.1 cache-aligned body ordering (shared-preamble first).
  </action>
  <verify>
    <automated>node -e "
    const fs = require('fs');
    const text = fs.readFileSync('agents/design-update-checker.md', 'utf8');
    const fm = text.match(/^---\n([\s\S]*?)\n---/);
    if (!fm) { console.error('missing frontmatter'); process.exit(1); }
    const body = fm[1];
    const required = ['name:', 'description:', 'tools:', 'color:', 'model:', 'default-tier:', 'parallel-safe:', 'reads-only:', 'writes:'];
    for (const k of required) if (!body.includes(k)) { console.error('missing: '+k); process.exit(2); }
    if (!/default-tier:\s*haiku/.test(body)) { console.error('tier not haiku'); process.exit(3); }
    if (!/writes:\s*\[\]/.test(body)) { console.error('writes not empty'); process.exit(4); }
    if (!/reads-only:\s*true/.test(body)) { console.error('not reads-only'); process.exit(5); }
    if (!/@reference\/shared-preamble\.md/.test(text)) { console.error('no preamble'); process.exit(6); }
    console.log('ok');
    "</automated>
  </verify>
  <acceptance_criteria>
    - `agents/design-update-checker.md` exists
    - Frontmatter contains `name: design-update-checker`
    - Frontmatter contains `default-tier: haiku` AND `model: haiku`
    - Frontmatter contains `writes: []` AND `reads-only: true`
    - First non-blank body line is `@reference/shared-preamble.md` (Phase 10.1 D-17)
    - Body contains `## Role`, `## Required Reading`, `## Output contract`, `## Do Not`, `## Completion marker`
    - File size ≤ 250 lines (M tier): `wc -l agents/design-update-checker.md` returns a first field ≤ 250
    - `node tests/agent-frontmatter.test.cjs 2>&1 | grep -q FAIL` returns non-zero (i.e. grep FAILs to find FAIL — the agent passes frontmatter tests). If the test runner name differs, run `npm test -- --filter=agent-frontmatter` instead.
    - `grep -q 'UPDATE-CHECKER COMPLETE' agents/design-update-checker.md` — completion marker present
  </acceptance_criteria>
  <done>
    agents/design-update-checker.md is committable, Haiku-tiered, reads-only, passes the Phase 12 frontmatter + size-budget tests, and declares a clean inline-text-only contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM input → user-visible text | Release body from GitHub is fed to the agent; agent output is rendered in the user's terminal via the spawning skill |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.3.03-01 | Injection (prompt-injection payload hidden in release body) | design-update-checker Task prompt | mitigate | Agent has only Read/Grep/Glob tools (no Write, no WebFetch, no Bash) — cannot be tricked into executing anything; `reads-only: true`; Phase 7 injection-scanner CI gate already scrubs agent files for known dangerous patterns |
| T-13.3.03-02 | Information disclosure | agent output → terminal | accept | The agent output is a summary of a public GitHub release; no secrets flow through it |
| T-13.3.03-03 | Quality (over-budget spawn on Sonnet by mistake) | default-tier frontmatter | mitigate | `default-tier: haiku` + Phase 10.1 budget-enforcer hook route this agent to Haiku; `.design/budget.json` tier_overrides default (empty) is fine — explicit frontmatter pin is authoritative |
</threat_model>

<verification>
- `node -e "..."` validation block above exits 0
- `wc -l agents/design-update-checker.md` first field ≤ 250
- `npm test -- --test-name-pattern=agent` (or Phase 12's equivalent) passes with the new agent present
</verification>

<success_criteria>
- design-update-checker agent ships, Haiku-tiered, reads-only, inline-text-only
- Frontmatter passes the Phase 12 validator
- Size budget passes (M tier)
- Agent consumes `.design/update-cache.json` and a prompted release body, returns a 3–5-line user-facing summary, ends with the `UPDATE-CHECKER COMPLETE` marker
</success_criteria>

<output>
After completion, create `.planning/phases/13.3-plugin-update-checker/13.3-03-SUMMARY.md` documenting the agent path, exact frontmatter, Phase 12 validator output, and a hand-traced example of what the agent would return for a mock release body.
</output>
