---
phase: 13.3-plugin-update-checker
plan: 01
title: "Schema extension for update_dismissed field"
type: execute
wave: 1
depends_on: []
requirements: []
files_modified:
  - reference/schemas/config.schema.json
autonomous: true
tags: [schema, config, update-checker]
must_haves:
  truths:
    - "config.schema.json accepts an optional string property `update_dismissed` at the top level"
    - "A .design/config.json containing `update_dismissed: \"v1.0.7.3\"` passes `scripts/validate-schemas.cjs` with exit 0"
    - "A .design/config.json with an integer (not string) `update_dismissed` fails schema validation"
    - "Existing config.json keys (model_profile, parallelism) still validate unchanged"
  artifacts:
    - path: reference/schemas/config.schema.json
      provides: "Schema entry for optional update_dismissed string"
      contains: "update_dismissed"
  key_links:
    - from: reference/schemas/config.schema.json
      to: scripts/validate-schemas.cjs
      via: "ajv-cli invocation in CI validate job"
      pattern: "update_dismissed"
---

<objective>
Add an optional `update_dismissed` string property to `reference/schemas/config.schema.json` so downstream 13.3 code can persist a per-version dismissal flag without tripping Phase 13's schema-validation CI gate.

Purpose: unblocks plans 13.3-02 (hook writes the flag) and 13.3-04 (`/gdd:check-update --dismiss` writes the flag) — both must produce config.json that passes `scripts/validate-schemas.cjs` in CI.
Output: one file touched, one-line schema addition, green schema validation against a sample config carrying the new field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.3-plugin-update-checker/13.3-CONTEXT.md
@reference/schemas/config.schema.json

<interfaces>
Current config.schema.json (draft-07, top-level `type: object`, `additionalProperties: true`) only declares two property shapes: `model_profile` (enum) and `parallelism` (nested object). The phase adds exactly one sibling: `update_dismissed` typed as `string`.

No existing key conflicts — grep `update_dismissed` in the repo before editing returns zero hits.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add update_dismissed string property to config.schema.json</name>
  <read_first>
    - reference/schemas/config.schema.json (the file being edited)
    - scripts/validate-schemas.cjs (to confirm which schemas are exercised in CI and which fixture runner would flag a regression)
  </read_first>
  <action>
    Edit `reference/schemas/config.schema.json`. Inside the top-level `properties` object, add a new sibling key after `parallelism`:

    ```json
    "update_dismissed": {
      "type": "string",
      "description": "Latest plugin tag (e.g. \"v1.0.7.3\") whose update nudge the user has dismissed. Set by /gdd:check-update --dismiss and by hooks/update-check.sh on the --dismiss code path. When a newer tag ships, the nudge reappears."
    }
  ```

    Final structure — keep `additionalProperties: true` unchanged, keep existing keys unchanged. Only insert the new property block.

    Do NOT add `"required": ["update_dismissed"]` — this field is optional (users who never dismiss never write it).

    Do NOT change the schema `$id`, `$schema`, or `title`.

    Also search the repo to confirm no stale reference to a conflicting field exists:

    ```bash
    grep -rn "update_dismissed" --include="*.md" --include="*.json" --include="*.cjs" --include="*.js" --include="*.sh" . | grep -v ".planning/" | grep -v "node_modules"
    ```

    If matches are found outside of the schema change and 13.3 plans, halt and investigate — otherwise proceed.

    Implements D-21 (schema addition) and establishes the storage contract for D-13 (per-version dismissal) + D-14 (atomic config write).
  </action>
  <verify>
    <automated>node -e "const s=require('./reference/schemas/config.schema.json'); if(!s.properties.update_dismissed) process.exit(1); if(s.properties.update_dismissed.type!=='string') process.exit(2); console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"update_dismissed"' reference/schemas/config.schema.json` returns at least 1
    - `grep -c '"type": "string"' reference/schemas/config.schema.json` includes the new entry (value ≥ 1)
    - `node scripts/validate-schemas.cjs` exits 0 (Phase 13 schema-validation CI reproduction)
    - Ad-hoc fixture test: `echo '{"model_profile":"balanced","update_dismissed":"v1.0.7.3"}' | node -e "const Ajv=require('ajv'); const ajv=new Ajv({strict:false}); const s=require('./reference/schemas/config.schema.json'); const v=ajv.compile(s); let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{ const o=JSON.parse(d); if(!v(o)){console.error(v.errors);process.exit(1)} console.log('valid'); });"` prints `valid` (If ajv is not installed, fall back to invoking `scripts/validate-schemas.cjs`'s structural-parse path — the schema is still JSON-parseable, which is the minimum CI gate.)
  </acceptance_criteria>
  <done>
    config.schema.json has the new property, parses as valid JSON, and a sample config carrying `update_dismissed: "v1.0.7.3"` validates clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → CI | Schema file is read by CI at validate step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.3.01-01 | Tampering | reference/schemas/config.schema.json | accept | Repo-level branch protection + code review covers tampering; schema is public non-secret JSON |
| T-13.3.01-02 | Denial of Service | CI validate job | mitigate | Schema change must JSON-parse — the node -e check in verify guarantees this before commit; malformed JSON would fail CI fast |
</threat_model>

<verification>
- `node scripts/validate-schemas.cjs` exits 0 against the modified schema (confirms ajv still compiles it)
- `git diff reference/schemas/config.schema.json` shows exactly one added property block, no unrelated edits
</verification>

<success_criteria>
- config.schema.json declares `update_dismissed` as an optional top-level `type: string` property
- Sample `{"update_dismissed": "v1.0.7.3"}` validates clean
- Phase 13 CI schema-validation gate passes on the change (simulated locally via scripts/validate-schemas.cjs)
</success_criteria>

<output>
After completion, create `.planning/phases/13.3-plugin-update-checker/13.3-01-SUMMARY.md` summarizing the single-file schema change, the exact JSON block added, and the verify-command transcript.
</output>
