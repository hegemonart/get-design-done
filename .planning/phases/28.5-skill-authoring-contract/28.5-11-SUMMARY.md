---
phase: 28.5
plan: 11
subsystem: ci
tags: [ci, validator-gate, health-report, regression-baseline, wave-d-sequential]
requires: ["28.5-01", "28.5-04", "28.5-05", "28.5-06", "28.5-07", "28.5-08", "28.5-09", "28.5-10"]
provides:
  - ".github/workflows/ci.yml lint job — runs validate-skill-length on every push/PR (exit 2 fails build, exit 1 advisory)"
  - "skills/health/SKILL.md — skill-length report subsection (5 new lines describing the output contract)"
  - "reference/health-skill-length-report.md — JSON shape + render contract docs (60 lines)"
  - "test-fixture/baselines/phase-28.5/skill-length-distribution.txt — 70-skill line-count snapshot"
  - "test-fixture/baselines/phase-28.5/validator-summary.txt — locked {total:70, clean:70, warn:0, block:0}"
  - "tests/phase-28.5-baseline.test.cjs — 9 regression assertions (±5 line tolerance)"
  - "tests/phase-28.5-ci-integration.test.cjs — 6 ci.yml assertions"
  - "tests/phase-28.5-health-skill-report.test.cjs — 6 health-skill assertions"
affects:
  - "CI lint job step count: 8 -> 9 (additive only)"
  - "skills/health/SKILL.md line count: 74 -> 83 (≤99 D-01 cap preserved)"
  - "Total shipped tests count: +21 in Phase 28.5-11 alone (6 CI + 6 health + 9 baseline)"
tech-stack:
  added: []
  patterns:
    - "Exit-code-discriminated CI step: capture validator exit via `EXIT=$?` after `set +e`, distinguish 0/1/2 via shell guard, emit GitHub Actions ::error:: vs ::warning:: annotations"
    - "Version-agnostic baseline test (Phase 28 D-08): no literal v1.28.5 encoded; reads validator output dynamically; ±5 line drift tolerance"
    - "Full RegExp escape in dynamic test regexes (Phase 28 lesson 5, CodeQL js/incomplete-sanitization)"
key-files:
  created:
    - reference/health-skill-length-report.md
    - test-fixture/baselines/phase-28.5/skill-length-distribution.txt
    - test-fixture/baselines/phase-28.5/validator-summary.txt
    - tests/phase-28.5-baseline.test.cjs
    - tests/phase-28.5-ci-integration.test.cjs
    - tests/phase-28.5-health-skill-report.test.cjs
  modified:
    - .github/workflows/ci.yml
    - skills/health/SKILL.md
decisions:
  - "CI step uses `set +e` + `EXIT=$?` pattern (not `|| true`) to distinguish exit 1 from exit 2 — needed because exit 1 must surface as ::warning:: while exit 2 must fail the build via explicit `exit 2`"
  - "STRICT_DESCRIPTION stays OFF in CI per D-02 — Phase 33 A/B evidence will graduate strict-form regex from advisory to hard-block"
  - "Health skill cross-links to reference/health-skill-length-report.md instead of inlining the JSON shape, keeping SKILL.md within the D-01 ≤99-line band (83 lines after edit; headroom 16)"
  - "Baseline tolerance ±5 lines per skill — permits minor refactors without churn; rigid exact-match would over-fire on cosmetic edits"
  - "New skills added post-baseline NOT flagged as regressions; removed skills ARE flagged (regression test discriminates explicitly)"
metrics:
  duration_minutes: 18
  tasks_completed: 4
  files_created: 6
  files_modified: 2
  commits: 7
  tests_added: 21
  tests_passing: 21
  validator_global: "70/70/0/0 (unchanged from start; rework already complete)"
completed: 2026-05-18
---

# Phase 28.5 Plan 11: CI Gate + Health Skill-Length Report + Regression Baseline Summary

**One-liner:** Wired `validate-skill-length.cjs` into the CI lint job (exit 2 fails build, exit 1 advisory), extended `skills/health/SKILL.md` with a 5-line skill-length report subsection (cross-linked to a new 60-line reference doc), and locked the post-Wave-A/B/C distribution (70 skills, 70 clean, 0 warn, 0 block) via 9 regression assertions in `tests/phase-28.5-baseline.test.cjs`.

## Outcome

The Phase 28.5 skill-authoring contract now has a load-bearing CI gate per D-11. Every PR's lint job runs the validator, blockers fail the build via exit code 2 + `::error::` annotation, and warnings surface as `::warning::` annotations without failing. The health skill output now includes a 2-line skill-length distribution summary so users can see contract drift at any time (clean/warn/block counts plus per-blocker list when present). A baseline snapshot at `test-fixture/baselines/phase-28.5/` locks in the current line-count distribution; future PRs that drift a skill more than ±5 lines from baseline, regress a clean skill into warn/block, or remove a baseline-listed skill will fail `tests/phase-28.5-baseline.test.cjs`.

## Tasks Completed

| Task | Name | Commit(s) | Files |
|------|------|-----------|-------|
| 1 | CI yaml — add validate-skill-length step to lint job | `af0ce34` (RED), `a8dbaca` (GREEN) | `.github/workflows/ci.yml`, `tests/phase-28.5-ci-integration.test.cjs` |
| 2 | Health SKILL.md — add skill-length report subsection | `95c4056` (RED), `bb61e0f` (GREEN), `aa53da6` (MD038 fix) | `skills/health/SKILL.md`, `reference/health-skill-length-report.md`, `tests/phase-28.5-health-skill-report.test.cjs` |
| 3 | Regression baseline + test | `b1fc44c` (RED), `abe933a` (GREEN) | `tests/phase-28.5-baseline.test.cjs`, `test-fixture/baselines/phase-28.5/skill-length-distribution.txt`, `test-fixture/baselines/phase-28.5/validator-summary.txt` |
| 4 | Human-verify checkpoint | (auto-approved per user "work without stopping" directive) | n/a |

## Distribution Snapshot (post-Wave-A/B/C)

- **70 skills total** (was 69 pre-28.5-10; the new `zoom-out` micro-skill from Wave B brought it to 70)
- **All 70 clean** — 0 warnings, 0 blockers
- **Mean: 70.3 lines | Median: 76 | P95: 96 | Min: 24 | Max: 98**
- **Top-5 longest:** check-update (98), extract-learnings (98), optimize (97), peers (96), verify (96)
- All 70 skills remain ≤ 99 lines (well under the D-01 warn threshold of 100; max 98 leaves a 2-line buffer to warn, 152 lines to block).

Head of `skill-length-distribution.txt`:

```
add-backlog: 48
analyze-dependencies: 95
apply-reflections: 73
audit: 66
bandit-status: 94
benchmark: 65
brief: 95
cache-manager: 66
check-update: 98
compare: 82
...
```

Tail (zoom-out is the newest entry):

```
update: 37
verify: 96
warm-cache: 81
watch-authorities: 82
zoom-out: 26
```

## CI step verification

- **Step name:** `Validate skill-authoring contract`
- **Job:** `lint` (ubuntu-latest, Node 22)
- **Position:** After `Detect stale references` (preserves existing lint-job ordering; Node setup + npm ci already complete)
- **Runtime budget:** validator is O(n) over ~70 SKILL.md files of ~70 lines each ≈ <10s wall-clock per T-28.5-11-02 (DoS) acceptance
- **Annotation surface:** `::error::Skill-authoring contract blockers present (exit 2)` when blockers > 0; `::warning::Skill-authoring contract warnings present (advisory; exit 1)` when warnings > 0 and blockers = 0
- **No `continue-on-error: true`** — block exits must fail the build per D-11
- **No `STRICT_DESCRIPTION=1`** — strict regex stays OFF per D-02 until Phase 33 A/B evidence

Lint job step count: 8 → 9 (additive).

## Tests Added

**6 in `tests/phase-28.5-ci-integration.test.cjs`** — yaml references validator, --quiet flag used, yaml parses (js-yaml), step in lint job, no continue-on-error: true, no STRICT_DESCRIPTION.

**6 in `tests/phase-28.5-health-skill-report.test.cjs`** — SKILL.md mentions skill-length, references validator script, ≤99 lines, name: gdd-health preserved, disable-model-invocation: true preserved, validator clean for health.

**9 in `tests/phase-28.5-baseline.test.cjs`** — distribution file exists, summary file exists, ≥60 entries, summary parseable JSON with int fields, summary records 0/0, current matches baseline summary, ±5 line drift respected, baseline skills present in current, exit code matches summary.

**Total Phase 28.5-11 tests: 21/21 passing.** Existing `tests/skill-authoring-contract.test.cjs` regression-clean (19/19 still pass).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] MD038 violations in skill-length subsection**
- **Found during:** Task 2 GREEN (verified via `npx markdownlint-cli2`)
- **Issue:** Two inline code spans starting with two leading spaces — `` `  - <name> (<lines> lines)` `` and `` `  All skills within contract.` `` — violated markdownlint MD038 no-space-in-code-span rule. Phase 28 lesson 4 (MD038 clean) applies.
- **Fix:** Dropped the leading two-space indent; described row prefix in prose: "list each blocker as a row `- <name> (<lines> lines)`. Else: print `All skills within contract.`"
- **Files modified:** `skills/health/SKILL.md`
- **Commit:** `aa53da6`

### Out-of-scope Discoveries (deferred, NOT fixed)

**`tests/gdd-health-mcp-row.test.cjs` — 2 pre-existing failures**
- `27.7-04: SKILL step references harness settings file + mcp_nudge dismissal`
- `27.7-04: SKILL contains at least 4 of 5 expected row strings verbatim`
- **Status:** Pre-existing. Confirmed at commit `4f3a3de` (before 28.5-11 started). The 28.5-06 health rework collapsed the verbatim 5-row MCP block into a one-line summary + cross-link; the 27.7-04 test expectations were not updated.
- **Tracking:** Logged in `.planning/phases/28.5-skill-authoring-contract/deferred-items.md`. Phase 28.5-12 closeout candidate or a separate follow-up. NOT in scope for 28.5-11 (CI gate + baseline).

## Auth gates

None — no external auth needed.

## STRIDE coverage

All 6 STRIDE threats from the plan documented:

| ID | Disposition | Status |
|----|-------------|--------|
| T-28.5-11-01 (Tampering of ci.yml) | mitigate | Mitigated — PR review surface; existing `permissions: contents: read` preserved |
| T-28.5-11-02 (DoS on validator) | mitigate | Mitigated — O(n) over 70 files of ~70 lines; <10s budget honored |
| T-28.5-11-03 (Elevation of priv) | accept | Accepted — GitHub Actions sandbox isolation; ephemeral user |
| T-28.5-11-04 (Spoofing baselines) | mitigate | Mitigated — baselines in git; PR diff surfaces tampering; regression test catches |
| T-28.5-11-05 (Info disclosure in CI logs) | accept | Accepted — output is names + line counts + error codes only |
| T-28.5-11-06 (Repudiation) | mitigate | Mitigated — `::error::` / `::warning::` annotations + JSON in stdout audit-traceable |

## Self-Check: PASSED

Files created (6):
- FOUND: reference/health-skill-length-report.md
- FOUND: test-fixture/baselines/phase-28.5/skill-length-distribution.txt
- FOUND: test-fixture/baselines/phase-28.5/validator-summary.txt
- FOUND: tests/phase-28.5-baseline.test.cjs
- FOUND: tests/phase-28.5-ci-integration.test.cjs
- FOUND: tests/phase-28.5-health-skill-report.test.cjs

Files modified (2):
- VERIFIED: .github/workflows/ci.yml — has `Validate skill-authoring contract` step in lint job; yaml parses
- VERIFIED: skills/health/SKILL.md — 83 lines; has `## Skill-length report` subsection; markdownlint clean

Commits (7):
- FOUND: af0ce34 test(28.5-11): RED — assert ci.yml lint job invokes validate-skill-length
- FOUND: a8dbaca feat(28.5-11): wire validate-skill-length into CI lint job (D-11)
- FOUND: 95c4056 test(28.5-11): RED — health SKILL.md skill-length report subsection
- FOUND: bb61e0f feat(28.5-11): add skill-length report subsection to health skill
- FOUND: aa53da6 fix(28.5-11): MD038 — remove spaces inside inline code spans in health skill
- FOUND: b1fc44c test(28.5-11): RED — Phase 28.5 regression baseline test (9 assertions)
- FOUND: abe933a test(28.5-11): GREEN — Phase 28.5 baseline snapshot (70 skills, 0 warn/block)

Verification block (plan):
- [1] CI yaml valid + has step → OK
- [2] Health skill ≤99 lines → OK (83)
- [3] Health skill has skill-length subsection → OK
- [4] Baseline files present → OK (distribution + summary)
- [5] Regression test passes → OK (9/9)
- [6] Post-rework state — 0 blockers + 0 warnings → OK (70/70/0/0)
