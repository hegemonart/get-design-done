---
phase: 28.5
plan: 12
subsystem: closeout
tags: [closeout, manifests, changelog, notice, off-cadence, baseline, roadmap-flip, wave-d-final, mit-attribution]
requires:
  - "28.5-01 through 28.5-11 (all 11 feature plans)"
provides:
  - "4-manifest lockstep at v1.28.5"
  - "OFF_CADENCE_VERSIONS.add('1.28.5')"
  - "CHANGELOG ## [1.28.5] block"
  - "NOTICE Phase 28.5 MIT attribution"
  - "24 new reference/registry.json entries (phase: 28.5)"
  - "registry.schema.json phase field: integer -> number alignment"
  - "test-fixture/baselines/phase-28.5/{manifests-version, cross-link-integrity, registry-diff}.txt"
  - "test-fixture/baselines/phase-20/skill-list.txt += zoom-out (70 skills)"
  - "test-fixture/baselines/phase-28/manifests-version.txt: 1.28.0 -> 1.28.5"
  - "test-fixture/baselines/phase-27-7/manifests-version.txt: 1.28.0 -> 1.28.5"
  - "test-fixture/baselines/current/skill-list.txt += zoom-out"
  - "ROADMAP Phase 28.5 plan checkboxes flipped (12 inline + 1 overview)"
affects:
  - "deferred test failures from 28.5-06 health rework: gdd-health-mcp-row + utility-skill health re-anchored"
  - "byte-for-byte after.md baseline drift across pipeline-stage skills (brief/design/explore/plan/verify) regenerated"
  - "byte-for-byte after.md baseline drift across utility skills (pause/resume/progress/health/todo/settings) regenerated"
  - "content-preservation tests re-anchored to read SKILL + linked reference graph together (precedent: 3454d68)"
tech-stack:
  added: []
  patterns:
    - "SKILL + reference progressive-disclosure verification: tests assert content presence across the combined surface (SKILL.md + linked reference/*-procedure.md) rather than SKILL.md only, accommodating Phase 28.5 extraction without losing the original assertion intent"
key-files:
  created:
    - "test-fixture/baselines/phase-28.5/manifests-version.txt"
    - "test-fixture/baselines/phase-28.5/cross-link-integrity.txt"
    - "test-fixture/baselines/phase-28.5/registry-diff.txt"
  modified:
    - "package.json (version 1.28.0 -> 1.28.5)"
    - ".claude-plugin/plugin.json (version 1.28.0 -> 1.28.5)"
    - ".claude-plugin/marketplace.json (metadata.version + plugins[0].version 1.28.0 -> 1.28.5)"
    - "test-fixture/baselines/phase-28/manifests-version.txt (1.28.0 -> 1.28.5)"
    - "test-fixture/baselines/phase-27-7/manifests-version.txt (1.28.0 -> 1.28.5)"
    - "test-fixture/baselines/phase-20/skill-list.txt (69 -> 70 skills, zoom-out appended)"
    - "test-fixture/baselines/current/skill-list.txt (69 -> 70 skills)"
    - "tests/semver-compare.test.cjs (OFF_CADENCE_VERSIONS.add('1.28.5'))"
    - "CHANGELOG.md (## [1.28.5] block at top)"
    - "NOTICE (Phase 28.5 mattpocock/skills MIT attribution block)"
    - "reference/registry.json (24 new phase: 28.5 entries)"
    - "reference/registry.schema.json (phase field: integer -> number alignment)"
    - "SKILL.md root (argument-hint += zoom-out)"
    - ".planning/ROADMAP.md (12 inline plan checkboxes + 1 overview entry flipped)"
    - "10 test files re-anchored (preservation tests now read SKILL + linked reference surface)"
    - "6 utility-skills + 5 pipeline-stage after.md baselines regenerated"
decisions:
  - "D-12 — 4-manifest lockstep at v1.28.5 + OFF_CADENCE_VERSIONS.add('1.28.5'). Off-cadence decimal patch from v1.28.0 parent per CONTEXT.md convention."
  - "D-03 closeout — NOTICE Phase 28.5 attribution block covers 5 mattpocock-derived reference ports + zoom-out direct port + debug Phase 1 ordering adoption (MIT license)."
  - "registry.schema.json phase field aligned from 'integer' -> 'number' to match long-standing data precedent (phase: 19.6 / 27.5 / 27.6 / 27.7 entries already shipped); documentation alignment, not Rule-4 schema redesign."
  - "Closeout cleanup precedent (3454d68): regenerate byte-for-byte after.md baselines for SKILL.md files reworked in 28.5-04..06, AND re-anchor line-count + content-preservation assertions to read SKILL + linked reference graph together as the canonical surface. Pattern is repeatable for future closeouts when progressive-disclosure extraction lands."
metrics:
  duration_minutes: 28
  completed: 2026-05-18
---

# Phase 28.5 Plan 12: Closeout — Lockstep ship of v1.28.5 (Wave D final)

Closes Phase 28.5 by shipping v1.28.5: 4-manifest lockstep, off-cadence version registration, CHANGELOG block at top, NOTICE MIT attribution for mattpocock/skills (5 ports + zoom-out + debug Phase 1 ordering), 24 new `reference/registry.json` entries, 5-file regression baseline at `test-fixture/baselines/phase-28.5/`, scoped ROADMAP flip (12 plans + 1 overview), and resolution of 86 baseline-drift test failures down to 0. `npm test`: 2247 tests / 2227 pass / 0 fail / 20 skipped.

## What changed

### 4-manifest lockstep at v1.28.5

| Manifest | From | To |
|----------|------|-----|
| `package.json#version` | 1.28.0 | **1.28.5** |
| `.claude-plugin/plugin.json#version` | 1.28.0 | **1.28.5** |
| `.claude-plugin/marketplace.json#metadata.version` | 1.28.0 | **1.28.5** |
| `.claude-plugin/marketplace.json#plugins[0].version` | 1.28.0 | **1.28.5** |
| `test-fixture/baselines/phase-28/manifests-version.txt` | 1.28.0 | **1.28.5** (Phase 28 baseline propagates forward per closeout discipline) |
| `test-fixture/baselines/phase-27-7/manifests-version.txt` | 1.28.0 | **1.28.5** (dynamic version-tracking baseline; Phase 27.7 fix precedent) |

`tests/semver-compare.test.cjs`: `OFF_CADENCE_VERSIONS.add('1.28.5')` added with rationale comment.

### CHANGELOG `## [1.28.5]` block at top

New entry placed above `## [1.28.0]` per closeout convention:

- Skill-authoring contract + validator + CI gate (28.5-01..03 + 11)
- 4 buckets of skill rework (28.5-04..07): 69 skills brought under 100 lines
- 21+ new reference files extracted per progressive-disclosure rule
- discuss + brief CONTEXT.md inline-write patches + decision-injector extension (28.5-08)
- debug Phase 1 feedback-loop gate (28.5-09)
- New `/gdd:zoom-out` micro-skill (28.5-10) — total skills 69 -> 70
- D-09 whitelist: 28 skills marked `disable-model-invocation: true`
- CI gate (28.5-11): validate-skill-length.cjs wired into ci.yml lint job + health subsection + Phase 28.5 regression baseline
- Closeout (28.5-12): 4-manifest lockstep + NOTICE attribution + registry diff + ROADMAP flip
- 12 D-XX decisions locked; out-of-scope items documented

markdownlint-cli2: 0 errors on both CHANGELOG.md and NOTICE.

### NOTICE Phase 28.5 MIT attribution

Added Phase 28.5 block to `NOTICE` covering:

- `reference/skill-authoring-contract.md` — adapted from mattpocock's `productivity/write-a-skill/SKILL.md`
- `reference/context-md-format.md` — adapted from `productivity/grill-with-docs/CONTEXT-FORMAT.md`
- `reference/adr-format.md` — adapted from `productivity/grill-with-docs/ADR-FORMAT.md`
- `reference/architecture-vocabulary.md` — ports Ousterhout glossary via `improve-codebase-architecture/LANGUAGE.md`
- `reference/debug-feedback-loops.md` — adapts `engineering/diagnose` Phase 1 (10 construction paths + iterate-on-loop + non-determinism branch)
- `skills/zoom-out/SKILL.md` — direct port of `engineering/zoom-out`
- `skills/debug/SKILL.md` Phase 1 ordering — adopts feedback-loop-discipline-first ordering

License: MIT (mattpocock/skills) → MIT (get-design-done). Straightforward re-licensing; attribution provides required source citation.

### registry.json += 24 new Phase 28.5 entries

Added 24 entries with `phase: 28.5` (numeric — decimal-phase precedent established by entries for phases 19.6 / 27.5 / 27.6 / 27.7):

| Bucket | Entries |
|--------|---------|
| MIT-attributed ports | adr-format, architecture-vocabulary, context-md-format, debug-feedback-loops, skill-authoring-contract |
| Bucket 1 (pipeline-stage) | design-procedure, discover-procedure, explore-procedure, plan-procedure, scan-procedure, verify-procedure |
| Bucket 2 (design-family) | compare-rubric, connections-onboarding, darkmode-audit-procedure, style-doc-procedure |
| Bucket 3 (orchestrator + utility) | apply-reflections-procedure, cache-policy, health-mcp-detection, router-rules, start-procedure |
| Bucket 4 (analysis + audit) | milestone-completeness-rubric, peer-cli-protocol, threat-modeling |
| CI subsection contract | health-skill-length-report |

`reference/registry.schema.json` updated: `phase` field type `integer` -> `number` to align with the long-standing data shape (precedent — decimal phase values shipped from Phase 19.6 onward; documentation alignment, not Rule-4 schema redesign).

Registry round-trip + smoke 14.5 + schema-validation tests all pass.

### `test-fixture/baselines/phase-28.5/` 5-file regression baseline

| File | Origin |
|------|--------|
| `skill-length-distribution.txt` | 28.5-11 (validator distribution snapshot) |
| `validator-summary.txt` | 28.5-11 (70 total / 70 clean / 0 warn / 0 block) |
| `manifests-version.txt` | 28.5-12 (1.28.5) |
| `cross-link-integrity.txt` | 28.5-12 (50 cross-links across 143 reference/*.md files, 0 MISSING) |
| `registry-diff.txt` | 28.5-12 (24 phase: 28.5 entries, alphabetic) |

### `test-fixture/baselines/phase-20/skill-list.txt` += zoom-out

69 → 70 skills (alphabetic order maintained; `zoom-out` appended after `watch-authorities`).

Also bumped:
- `test-fixture/baselines/current/skill-list.txt`: 69 → 70
- Root `SKILL.md` argument-hint: added `|zoom-out` for command-count-sync test compliance

### ROADMAP scoped flip

`.planning/ROADMAP.md`:

- Phase 28.5 section (lines 1656..1714): 12 inline plan checkboxes flipped `[ ]` → `[x]` for `28.5-01-PLAN.md` through `28.5-12-PLAN.md`
- Top-level overview entry (line 90): `- [ ] [Phase 28.5]...` → `- [x] [Phase 28.5]...`

Phase 28 section ([x] count = 7, [ ] count = 0): **unchanged** (no out-of-scope flips). Regex used hardcoded plan pattern `28\.5-(0[1-9]|1[0-2])-PLAN\.md` with leading-whitespace tolerance per Phase 28 lesson 5.

### 86 → 0 baseline-drift test failures resolved

Phase 28.5-04..06 reworked 4 Buckets of SKILL.md files to comply with the <=100-line authoring contract, extracting verbatim procedure content to `reference/*-procedure.md`. This drift broke 86 pre-existing migration-test baselines and content-preservation assertions. Per the precedent commit `3454d68` (Phase 27.7-07 closeout cleanup), the fix is:

1. **Regenerate byte-for-byte `after.md` baselines** for all reworked SKILL.md files (live = baseline) — applied to 6 utility skills (`pause`, `resume`, `progress`, `health`, `todo`, `settings`) + 5 pipeline-stage skills (`brief`, `design`, `explore`, `plan`, `verify`).
2. **Re-anchor line-count tolerance assertions** from `BEFORE_PATH` to `AFTER_PATH` in 5 migration tests (precedent from `3454d68`).
3. **Re-anchor content-preservation assertions** (worktree count, is_parallel forwarding, design-executor spawn count, STATE.md mutation protocol, parallelism-decision comment, update_progress count, update-in-place idiom, agent-name counts, 4-option setup prompt, S/M/L/XL router buckets, /gdd:help/scan/plan/next bucket assignments, Step 2.5 quality-gate gate, i18n-readiness states + library matrix + Intl.* regex, MCP-row strings + settings-file path) to read SKILL + linked `reference/*-procedure.md` together as the canonical surface. This honors the original assertion intent while accommodating the Phase 28.5 progressive-disclosure extraction.
4. **Edge cases:**
   - `tests/skill-design-mcp-migration.test.cjs` FORBIDDEN_STATE_MUTATION_PATTERNS regex line-scoped (`/Write[^\n`]*STATE\.md(?!`)/`) to avoid false-positive cross-paragraph match where "## Figma Write Dispatch\n\n...if STATE.md..." accidentally matched.
   - `tests/meta-rules-split.test.cjs` shared-preamble size ceiling raised from 4KB → 12KB (28.5-05 intentional re-extension to host cross-skill probe pattern + connection-handshake summary + output-contract reminders).
   - `tests/deprecation-redirect.test.cjs` ALLOWED_DEPRECATION_REFS += `reference/plan-procedure.md` (28.5-04 extracted the `design-pattern-mapper` Task() spawn site there).
   - `tests/command-count-sync.test.cjs` satisfied by argument-hint += `zoom-out` in root SKILL.md.

Result: `npm test` returns 2247 tests / 2227 pass / 0 fail / 20 skipped.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] 86 pre-existing test failures from 28.5 reworks resolved as part of closeout cleanup**

- **Found during:** Task 5 (resolve 2 deferred test failures from 28.5-06)
- **Issue:** The plan called out 2 deferred failures (`tests/gdd-health-mcp-row.test.cjs`); on full `npm test` the actual count was 86 across 18 test files. Pre-existing baseline-drift from 28.5-04..06 rework extractions had cascaded into more failing assertions than expected.
- **Fix:** Applied the closeout-cleanup precedent (commit `3454d68` from 27.7-07 + `39e402c` from 28-07): regenerated `after.md` byte-for-byte baselines and re-anchored line-count + content-preservation tests to read SKILL + linked reference as the canonical surface (`readSkillSurface()` helper pattern applied to design / plan / verify / connections / router-complexity-class / phase-25-baseline / phase-28-probes / gdd-health-mcp-row tests). Also addressed 3 edge-case adjacent failures (cross-paragraph regex false-positive in design test, shared-preamble size ceiling stale by 28.5-05 re-extension, plan-procedure.md missing from deprecation allow-list).
- **Files modified:** 10 test files, 11 baseline files
- **Commits:** `ab2a638` (pipeline-stage + utility-skill regen) + `11e33cf` (remaining 17 cleanup)

**2. [Rule 2 - Missing Critical] Phase-27-7 manifests-version.txt baseline bumped (dynamic version-tracking)**

- **Found during:** Task 7 regression sweep
- **Issue:** `test-fixture/baselines/phase-27-7/manifests-version.txt` asserts `baseline == package.json#version` dynamically. With package.json at 1.28.5 and the baseline still at 1.28.0, the test failed.
- **Fix:** Bumped baseline to 1.28.5. Phase 27.7 fix precedent (`51b15da` from 28-07 closeout) bumped this baseline to 1.28.0 for the same reason during Phase 28 closeout.
- **Files modified:** `test-fixture/baselines/phase-27-7/manifests-version.txt`
- **Commit:** `11e33cf`

**3. [Rule 1 - Bug] `test-fixture/baselines/current/skill-list.txt` also needed zoom-out**

- **Found during:** Task 5 regression
- **Issue:** Plan called for `phase-20/skill-list.txt` += zoom-out (which I did). On regression, `regression-baseline.test.cjs` also asserts against `current/skill-list.txt`.
- **Fix:** Added zoom-out to `test-fixture/baselines/current/skill-list.txt` (alphabetic position; 69 → 70).
- **Commit:** `11e33cf`

**4. [Rule 1 - Bug] Root SKILL.md argument-hint missing zoom-out**

- **Found during:** Task 5 regression
- **Issue:** `tests/command-count-sync.test.cjs` asserts that every `skills/<name>/` directory is mentioned somewhere in root SKILL.md. With zoom-out being a new skill, this test failed.
- **Fix:** Added `|zoom-out` to root SKILL.md `argument-hint` frontmatter. Minimal surface change; doesn't promote zoom-out to a "must mention" elsewhere in the body (it stays user-invoked-only per disable-model-invocation:true).
- **Commit:** `11e33cf`

### Items NOT taken (out of scope)

- No description-format regex tightening — deferred until Phase 33 A/B evidence on `<what>` clause shortcut behavior lands at `.design/research/description-format-ab.md` (CHANGELOG out-of-scope documents this).
- No tests/phase-28.5-baseline.test.cjs full-RegExp-escape audit; existing test code uses the safe `VERSION.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')` form per Phase 28 lesson 5.
- No version-sequence prose append to ROADMAP — existing Phase 28.5 section already documents 'Target version: v1.28.5' inline, and the top-level overview entry was the one flip the plan called for at the top of ROADMAP. (Plan's optional Step C wasn't strictly required since version-sequence prose convention in this repo lives inline-per-phase, not in a standalone section.)

## Authentication Gates

None — no auth gates triggered during this plan.

## Verification

| Check | Result |
|-------|--------|
| 4 manifests at v1.28.5 | OK (3 files + phase-28 baseline) |
| OFF_CADENCE_VERSIONS.add('1.28.5') | OK |
| semver-compare test passes | OK (3/3) |
| CHANGELOG [1.28.5] block | OK |
| NOTICE Phase 28.5 + mattpocock | OK |
| Registry 7+ required entries present | OK (24 phase-28.5 entries, all 7 mandatories included) |
| Validator clean | OK (70 total / 70 clean / 0 warn / 0 block) |
| Phase-28.5 baseline files (5) | OK (all present) |
| Cross-link integrity 0 MISSING | OK |
| Phase-20 skill-list += zoom-out | OK (70 lines) |
| ROADMAP 12 plan checkboxes flipped | OK |
| ROADMAP 0 [ ] remaining in Phase 28.5 | OK |
| Phase 28 section untouched | OK (7 [x], 0 [ ] — unchanged) |
| Phase 28.5 regression baseline test | OK (9/9 pass) |
| Skill-authoring-contract test | OK (19/19 pass) |
| Full npm test | **2247 tests / 2227 pass / 0 fail / 20 skipped** |

## Commits

- `73d01b9` chore(28.5-12): bump 4 manifests to 1.28.5 lockstep + OFF_CADENCE registration
- `b1889e7` docs(28.5-12): add CHANGELOG [1.28.5] entry + NOTICE mattpocock MIT attribution
- `4487d47` feat(28.5-12): register 24 new Phase 28.5 ref entries in registry.json
- `fd4548e` test(28.5-12): add zoom-out to phase-20 skill-list + phase-28.5 baseline files
- `ab2a638` fix(28.5-12): regen pipeline-stage + utility-skill after.md baselines + re-anchor preservation tests to SKILL+reference surface
- `11e33cf` fix(28.5-12): re-anchor remaining content-preservation tests + bump tracked baselines (npm test 0 failures)
- `490299a` docs(28.5-12): flip Phase 28.5 checkboxes (scoped + overview)

## Threat Surface Scan

Per the threat_model in the plan, the 7 STRIDE threats were registered with `mitigate` / `accept` dispositions. All mitigations were honored:

- T-28.5-12-01 (ROADMAP scoped flip Tampering) — scope-bounded by `^### Phase 28.5:` start + `^### Phase [0-9]` end markers; hardcoded plan-name regex; per-phase verification asserts 0 out-of-scope flips. OK.
- T-28.5-12-02 (Manifest version bumps Tampering) — plain string replacement; CI semver-compare passes. OK.
- T-28.5-12-06 (Off-cadence version Repudiation) — `OFF_CADENCE_VERSIONS.add('1.28.5')` committed with rationale comment; semver-compare audit-traceable. OK.
- T-28.5-12-07 (Phase 28 baseline propagation Spoofing) — baseline file is git-tracked; bump is single-line change visible in PR diff. OK.

No additional security-relevant surface introduced (no new endpoints, auth paths, file-access patterns, or schema changes at trust boundaries).

## Self-Check

| Claim | Evidence |
|-------|----------|
| `package.json#version` == 1.28.5 | grep returns 1 |
| `.claude-plugin/plugin.json#version` == 1.28.5 | grep returns 1 |
| `.claude-plugin/marketplace.json` 1.28.5 (2 places) | grep returns 2 |
| `CHANGELOG.md` has `## [1.28.5]` | grep returns 1 |
| `NOTICE` has Phase 28.5 block | grep "Phase 28.5" returns 2 |
| `reference/registry.json` has 24 phase: 28.5 entries | node script returns 24 |
| `test-fixture/baselines/phase-28.5/` has 5 files | ls returns 5 |
| `test-fixture/baselines/phase-20/skill-list.txt` line count | wc -l returns 70 |
| `.planning/ROADMAP.md` 12 plan checkboxes [x] in Phase 28.5 section | node verification returns x=12, b=0 |
| `npm test` full run | 2247 tests / 2227 pass / 0 fail / 20 skipped |

## Self-Check: PASSED

All deliverables shipped. v1.28.5 ship-ready.
