---
phase: 28.5
plan: 10
subsystem: skills
tags: [skill-new, zoom-out, micro-skill, disable-model-invocation, mit-port, mattpocock, wave-b]
requires: ["28.5-01"]
provides:
  - "skills/zoom-out/SKILL.md — new /gdd:zoom-out micro-skill (D-09 whitelist member)"
  - "tests/zoom-out.test.cjs — 7 tests guarding skill contract"
affects:
  - "Phase 20 baseline skill-list.txt (delta of +1; bump deferred to 28.5-12 closeout)"
  - "Global skill count: 69 → 70 clean"
tech-stack:
  added: []
  patterns:
    - "MIT-port pattern: attribution at body top + reference to NOTICE (D-03)"
    - "Micro-skill pattern: ~25 lines with disable-model-invocation: true (user-invoked-only)"
key-files:
  created:
    - skills/zoom-out/SKILL.md
    - tests/zoom-out.test.cjs
  modified: []
decisions:
  - "Description form: lax-default (D-02) — includes 'Use when...' but not strictly enforced; description length 229 chars (well within 20-1024 range)"
  - "Added argument-hint: '[scope]' — optional hint per plan's deliverables section even though not in plan body action"
  - "Body length 26 lines — within plan target (15-30) and well under 99-line warn threshold"
metrics:
  duration_minutes: 6
  tasks_completed: 2
  files_created: 2
  files_modified: 0
  commits: 2
  tests_added: 7
  tests_passing: 7
  validator_global: "70/0/0 (was 69/0/0)"
completed: 2026-05-18
---

# Phase 28.5 Plan 10: New /gdd:zoom-out Micro-Skill Summary

**One-liner:** New `/gdd:zoom-out` user-invoked-only micro-skill ported from mattpocock/skills (MIT) with `disable-model-invocation: true`, cross-linked to CONTEXT.md vocabulary and architecture-vocabulary seam terminology.

## Outcome

Added the canonical example of a Phase 28.5-compliant micro-skill: 26 lines total (frontmatter + MIT attribution + body), single instruction block ("go up a layer of abstraction, give me a map using the project's domain glossary"), D-09 whitelist match prevents router auto-fire. Validator stays clean (70/70 skills clean, 0 warnings, 0 blockers).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create skills/zoom-out/SKILL.md | `7ea42be` | `skills/zoom-out/SKILL.md` (+26 lines) |
| 2 | Author tests/zoom-out.test.cjs | `a09487a` | `tests/zoom-out.test.cjs` (+91 lines) |

## Skill Details

**File:** `skills/zoom-out/SKILL.md` (26 lines)

**Frontmatter:**
- `name: zoom-out` (matches directory)
- `description:` — 229 chars, lax-default form: "Asks the agent to go up a layer of abstraction and map the relevant modules and callers using the project's CONTEXT.md vocabulary. Use when the user is working in an unfamiliar area of code and needs orientation before deep work."
- `disable-model-invocation: true` (D-09 whitelist member — router must NOT auto-fire)
- `argument-hint: "[scope]"` (optional positional)

**Body content:**
- MIT attribution at top: `Source: mattpocock/skills (MIT) — adapted with permission. See \`../NOTICE\` for the full attribution block.`
- Single instruction: "I don't know this area of code well. Go up a layer of abstraction. Give me a map of all the relevant modules and callers, using the project's domain glossary (`CONTEXT.md`) vocabulary."
- 3-point map spec: modules in scope (with CONTEXT.md schema cross-link), callers, seams (with architecture-vocabulary cross-link)
- Fallback for absent CONTEXT.md: suggest `/gdd:discuss`, still produce best-effort map
- Closer: `## ZOOM-OUT COMPLETE`

## Test Details

**File:** `tests/zoom-out.test.cjs` (91 lines)

**Tests (7 total — plan required ≥3):**

1. `zoom-out skill file exists` — guards file path stability
2. `zoom-out has correct frontmatter` — opening `---`, `name:`, `description:` present
3. `zoom-out has disable-model-invocation: true (D-09 whitelist)` — critical: prevents router auto-fire
4. `zoom-out validates clean (whitelist applies, ≤99 lines)` — uses `validateSkill()` from 28.5-01 validator; asserts errors=0, warnings=0, level=clean, lines≤99
5. `zoom-out body references CONTEXT.md` — cross-reference to project glossary
6. `zoom-out body references architecture-vocabulary` — cross-reference to seam terminology
7. `zoom-out has MIT attribution (D-03)` — `Source: mattpocock/skills (MIT)` + NOTICE reference

All 7 tests pass on `node --test tests/zoom-out.test.cjs`.

## Validator Output

```
{
  "name": "zoom-out",
  "lines": 26,
  "descriptionLength": 229,
  "hasRequiredFields": true,
  "level": "clean",
  "errors": [],
  "warnings": [],
  "reasons": []
}
```

Global summary: `total=70, clean=70, warnings=0, blockers=0` (was 69/0/0 before this plan; +1 clean skill).

## Cross-Links Honored

| Link | From | To | Pattern |
|------|------|-----|---------|
| 1 | skills/zoom-out/SKILL.md | CONTEXT.md | `CONTEXT\.md` (4 occurrences) |
| 2 | skills/zoom-out/SKILL.md | reference/context-md-format.md | `./../reference/context-md-format.md` |
| 3 | skills/zoom-out/SKILL.md | reference/architecture-vocabulary.md | `./../reference/architecture-vocabulary.md` |
| 4 | skills/zoom-out/SKILL.md | NOTICE | `../NOTICE` |

## Deviations from Plan

**None** — plan executed exactly as written. Two minor enhancements within plan latitude:

- Added `argument-hint: "[scope]"` to frontmatter (plan's "Required deliverables" section listed it as optional; included for ergonomics).
- 7 tests delivered vs. 3+ required (plan explicitly allowed "≥3" and noted "we deliver 6" in its own body — we exceeded by one to add a separate test for `architecture-vocabulary` cross-reference for clearer regression detection).

## Auto-fixes Applied

None. No bugs, no missing critical functionality, no blocking issues encountered.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes. Skill is a static markdown file invoked only by user; tests are file-existence/grep checks. No threat flags raised.

## Deferred Items

**For 28.5-12 closeout (NOT this plan):**
- Bump `tests/fixtures/phase-20/skill-list.txt` to include `zoom-out` baseline (plan explicitly defers this).
- STATE.md / ROADMAP.md / registry.json / NOTICE updates (28.5-12 owns).

## Success Criteria — All Met

- [x] `skills/zoom-out/SKILL.md` created (new directory + file); frontmatter + attribution + body; 26 lines (≤30).
- [x] Validator clean (D-09 whitelist matched; `zoom-out` reports `level=clean`, errors=0, warnings=0).
- [x] 7 tests pass (≥3 required).
- [x] No collateral edits (`git status` clean; only 2 files in diff).
- [x] markdownlint clean (0 errors on `skills/zoom-out/SKILL.md`).

## Self-Check: PASSED

- File `skills/zoom-out/SKILL.md` exists: confirmed via `test -f`.
- File `tests/zoom-out.test.cjs` exists: confirmed via `test -f`.
- Commit `7ea42be` exists: confirmed via `git log`.
- Commit `a09487a` exists: confirmed via `git log`.
- Validator state matches claim (70/70 clean): confirmed via `validate-skill-length.cjs --quiet --json`.
- Test count matches claim (7 passing): confirmed via `node --test`.
