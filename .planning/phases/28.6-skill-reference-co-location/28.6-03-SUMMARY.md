---
phase: 28.6
plan: 03
subsystem: skill-reference-co-location
tags: [validator, baseline, npm-test, d-05, verification, re-anchor]
dependency-graph:
  requires: ["28.6-02 (registry purge + contract refresh — established the migration end-state being verified)"]
  provides: ["validated D-05 invariant, re-anchored test suite + fixtures, npm test green pre-closeout"]
  affects: ["28.6-04 (NOTICE + STATE + ROADMAP + manifest bump to v1.28.6 — needs a clean test bed before release)"]
tech-stack:
  added: []
  patterns:
    - "Test-path re-anchoring (Phase 27.7-07 / 28-XX closeout precedent — commits 3454d68, 39e402c)"
    - "Byte-for-byte fixture regen via direct cp from current SKILL.md (no test framework regeneration tooling exists)"
    - "ALLOWED_DEPRECATION_REFS allowlist migration (path string update, semantic intent preserved)"
key-files:
  created: []
  modified:
    - "test-fixture/baselines/phase-20/design-after.md (cross-link path strings refreshed from current skills/design/SKILL.md)"
    - "test-fixture/baselines/phase-20/explore-after.md (same — skills/explore/SKILL.md)"
    - "test-fixture/baselines/phase-20/utility-skills/health-after.md (same — skills/health/SKILL.md, 2 cross-links)"
    - "tests/connections-skill.test.cjs (refPath: reference/connections-onboarding.md -> skills/connections/connections-onboarding.md)"
    - "tests/debug-feedback-loop.test.cjs (REF: reference/debug-feedback-loops.md -> skills/debug/debug-feedback-loops.md)"
    - "tests/deprecation-redirect.test.cjs (ALLOWED_DEPRECATION_REFS: reference/plan-procedure.md -> skills/plan/plan-procedure.md)"
    - "tests/gdd-health-mcp-row.test.cjs (HEALTH_MCP_REF_PATH: reference/health-mcp-detection.md -> skills/health/health-mcp-detection.md)"
    - "tests/phase-25-baseline.test.cjs (2 refPath edits: router-rules + verify-procedure)"
    - "tests/phase-28-probes.test.cjs (procedurePath: reference/explore-procedure.md -> skills/explore/explore-procedure.md)"
    - "tests/router-complexity-class.test.cjs (ROUTER_RULES: reference/router-rules.md -> skills/router/router-rules.md)"
    - "tests/skill-design-mcp-migration.test.cjs (PROCEDURE_PATH: reference/design-procedure.md -> skills/design/design-procedure.md)"
    - "tests/skill-plan-mcp-migration.test.cjs (PROCEDURE_PATH: reference/plan-procedure.md -> skills/plan/plan-procedure.md)"
    - "tests/skill-verify-mcp-migration.test.cjs (PROCEDURE_PATH: reference/verify-procedure.md -> skills/verify/verify-procedure.md)"
  validator-touched: []
  baselines-touched:
    - "test-fixture/baselines/phase-20/design-after.md (re-anchored)"
    - "test-fixture/baselines/phase-20/explore-after.md (re-anchored)"
    - "test-fixture/baselines/phase-20/utility-skills/health-after.md (re-anchored)"
    - "test-fixture/baselines/phase-28.5/* (5 files — NOT touched; baseline test passed 9/9 without re-lock)"
decisions:
  - "D-05 verified by inspection + runtime: walkSkills() in scripts/validate-skill-length.cjs (lines 225-233) globs skills/<dir>/SKILL.md only. Co-located procedure refs at skills/<owner>/<topic>.md are invisible to the validator. Confirmed via git diff scripts/validate-skill-length.cjs against Phase 28.5 close (HEAD~7) -> 0 lines changed. Validator output: 70/70 clean, exit 0."
  - "Phase 28.5 baseline (test-fixture/baselines/phase-28.5/*) NOT re-locked. tests/phase-28.5-baseline.test.cjs passed 9/9 untouched — line counts unchanged across the path-string-only migration. Plan's expected outcome confirmed: no drift, no baseline write."
  - "Re-anchor scope: 10 test files + 3 phase-20 fixtures. All failures stemmed from cross-link path strings changing inside SKILL.md (and 1 ALLOWED_DEPRECATION_REFS entry); no test logic regressed."
metrics:
  duration: "~25 min"
  completed: "2026-05-18T18:14:00Z"
  tasks_completed: 3
  tasks_total: 3
  test_failures_pre_fix: 32
  test_failures_post_fix: 0
  test_pass_total: 2227
  test_total: 2247
  test_skipped: 20
  validator_skills: 70
  validator_clean: 70
  validator_warn: 0
  validator_block: 0
  fixtures_re_anchored: 3
  test_paths_re_anchored: 12
---

# Phase 28.6 Plan 03: Validator + Baseline Sweep Summary

**One-liner:** Verified D-05 (validator unchanged, globs SKILL.md only) at runtime + git-diff level; re-anchored 10 test files + 3 phase-20 byte-for-byte fixtures to point at co-located skill folders; full npm test sweep flipped from 32 failures -> 0 failures while validator stayed byte-identical to pre-28.6.

## Decisions Made

1. **D-05 verified two ways, no validator edits** — Inspection of `scripts/validate-skill-length.cjs` lines 225-233 confirmed `walkSkills()` globs only `<skillsDir>/<entry>/SKILL.md`; co-located procedure refs at `skills/<owner>/<topic>.md` cannot match the glob. Runtime confirmation: `node scripts/validate-skill-length.cjs --quiet` -> exit 0, summary `70 skills | 70 clean | 0 warn | 0 block`, identical to Phase 28.5 baseline. Git-diff against Phase 28.5 close (HEAD~7, `ae64740`): `git diff ae64740..HEAD -- scripts/validate-skill-length.cjs | wc -l` returns 0 lines. The validator file is byte-identical pre/post Phase 28.6.

2. **Phase 28.5 baseline NOT re-locked** — `tests/phase-28.5-baseline.test.cjs` passed 9/9 in isolation. The five `test-fixture/baselines/phase-28.5/*` files were left untouched (as expected). The plan's expected outcome was confirmed: path-string-only edits in SKILL.md do not change SKILL.md line counts, so no drift in `skill-length-distribution.txt`. `git status test-fixture/baselines/phase-28.5/` reports nothing modified.

3. **Phase-20 fixture re-anchor was unavoidable** — three byte-for-byte fixtures (`design-after.md`, `explore-after.md`, `utility-skills/health-after.md`) compare current SKILL.md to a frozen baseline. The Phase 28.6-01 cross-link path normalization changed bytes inside those SKILL.md files (e.g., `./reference/health-mcp-detection.md` -> `./health-mcp-detection.md`). Without regen, the byte-for-byte assertions failed. Regenerated by direct `cp skills/<owner>/SKILL.md test-fixture/baselines/phase-20/<owner>-after.md`. Per the test's own error message: "regen the fixture if the skill intentionally changed."

4. **Single commit per task category** — split into two commits per task_commit_protocol: (a) `fix(28.6-03): re-anchor 10 test paths after skill-private co-location` and (b) `test(28.6-03): regen 3 byte-for-byte fixtures after co-location`. The plan's optional Task 1 commit ("test(28.6-03): verify validator D-05 invariant after co-location") was intentionally skipped — verify-only with zero file edits, documented here instead per "possibly no-op commit if validator unchanged."

## Files Changed

### Tests re-anchored (Rule 1 — 13 test files / fixture files)

| Test File / Fixture                                          | Edit                                                                                | Commit  |
| ------------------------------------------------------------ | ----------------------------------------------------------------------------------- | ------- |
| `tests/connections-skill.test.cjs`                           | `refPath` -> `skills/connections/connections-onboarding.md`                         | e4e81f1 |
| `tests/debug-feedback-loop.test.cjs`                         | `REF` -> `skills/debug/debug-feedback-loops.md`                                     | e4e81f1 |
| `tests/deprecation-redirect.test.cjs`                        | `ALLOWED_DEPRECATION_REFS` entry -> `skills/plan/plan-procedure.md`                 | e4e81f1 |
| `tests/gdd-health-mcp-row.test.cjs`                          | `HEALTH_MCP_REF_PATH` -> `skills/health/health-mcp-detection.md`                    | e4e81f1 |
| `tests/phase-25-baseline.test.cjs`                           | 2 `refPath` edits: router-rules + verify-procedure                                  | e4e81f1 |
| `tests/phase-28-probes.test.cjs`                             | `procedurePath` -> `skills/explore/explore-procedure.md`                            | e4e81f1 |
| `tests/router-complexity-class.test.cjs`                     | `ROUTER_RULES` -> `skills/router/router-rules.md`                                   | e4e81f1 |
| `tests/skill-design-mcp-migration.test.cjs`                  | `PROCEDURE_PATH` -> `skills/design/design-procedure.md`                             | e4e81f1 |
| `tests/skill-plan-mcp-migration.test.cjs`                    | `PROCEDURE_PATH` -> `skills/plan/plan-procedure.md`                                 | e4e81f1 |
| `tests/skill-verify-mcp-migration.test.cjs`                  | `PROCEDURE_PATH` -> `skills/verify/verify-procedure.md`                             | e4e81f1 |
| `test-fixture/baselines/phase-20/design-after.md`            | regen from `skills/design/SKILL.md` (path strings only)                             | 09a087a |
| `test-fixture/baselines/phase-20/explore-after.md`           | regen from `skills/explore/SKILL.md` (path strings only)                            | 09a087a |
| `test-fixture/baselines/phase-20/utility-skills/health-after.md` | regen from `skills/health/SKILL.md` (path strings only)                         | 09a087a |

### Validator (verification only — D-05)

| File                                  | State                                                                       |
| ------------------------------------- | --------------------------------------------------------------------------- |
| `scripts/validate-skill-length.cjs`   | **UNCHANGED** (`git diff ae64740..HEAD -- ` returns 0 lines)                |

### Phase 28.5 baselines (verification only)

| File                                                                | State        | Reason                                          |
| ------------------------------------------------------------------- | ------------ | ----------------------------------------------- |
| `test-fixture/baselines/phase-28.5/skill-length-distribution.txt`   | **UNCHANGED** | Line counts identical pre/post co-location     |
| `test-fixture/baselines/phase-28.5/validator-summary.txt`           | **UNCHANGED** | Validator output identical (70/70 clean)       |
| `test-fixture/baselines/phase-28.5/cross-link-integrity.txt`        | **UNCHANGED** | No test asserted against this baseline as path-shape; assertions key off SKILL+ref combined surface |
| `test-fixture/baselines/phase-28.5/registry-diff.txt`               | **UNCHANGED** | Plan 28.6-02 already handled registry shape; no test that consumes this baseline failed |
| `test-fixture/baselines/phase-28.5/manifests-version.txt`           | **UNCHANGED** | Phase 28.6 has not yet bumped manifests (28.6-04 owns) |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] 32 npm test failures auto-fixed via test path re-anchoring**

- **Found during:** Task 3 (full `npm test` sweep)
- **Issue:** 32 tests failed in 10 test files because they hardcoded pre-28.6 `reference/<topic>.md` paths for the 8 procedure refs that Phase 28.6-01 moved to `skills/<owner>/<topic>.md`. One additional failure in `tests/deprecation-redirect.test.cjs` flagged `skills/plan/plan-procedure.md` as an orphan deprecation reference (the ALLOWED_DEPRECATION_REFS list still pointed at `reference/plan-procedure.md`).
- **Fix:** Updated each path constant + the deprecation allowlist entry to point at the co-located location.
- **Files modified:** 10 test files (see table above).
- **Commit:** e4e81f1

**2. [Rule 1 - Bug] 3 byte-for-byte fixture failures auto-fixed via regen**

- **Found during:** Task 3 (npm test sweep — same run as item 1)
- **Issue:** Phase-20 baselines `design-after.md`, `explore-after.md`, and `utility-skills/health-after.md` snapshot SKILL.md content byte-for-byte. Phase 28.6-01 changed cross-link path strings inside the SKILL.md files, causing exact-match assertions to fail. The test error messages explicitly instructed "regen the fixture if the skill intentionally changed."
- **Fix:** Direct `cp skills/<owner>/SKILL.md test-fixture/baselines/phase-20/...` for each of the three fixtures. Verified diff shows only the path-string lines change; frontmatter, prose, structure all preserved.
- **Files modified:** 3 fixture files.
- **Commit:** 09a087a

### No-op commit avoided

- The plan's optional `test(28.6-03): verify validator D-05 invariant after co-location` commit was intentionally skipped per task_commit_protocol ("only commit if there's a code change"). The verification result (validator unchanged, 70/70 clean) is documented in this SUMMARY under Decisions Made.

### No architectural changes (Rule 4 not triggered)

- All fixes were path-string and fixture-regen mechanics. No new tables, no new modules, no library swaps.

## Authentication Gates

None encountered.

## Known Stubs

None. All edits target real test code + real fixture files; no placeholders introduced.

## Threat Flags

None. No new network endpoints, auth paths, file access patterns, or schema changes.

## Verification

- [x] `node scripts/validate-skill-length.cjs --quiet` -> exit 0, summary `70 skills | 70 clean | 0 warn | 0 block` (identical to Phase 28.5 baseline)
- [x] `git diff ae64740..HEAD -- scripts/validate-skill-length.cjs | wc -l` -> 0 (validator byte-identical)
- [x] `node --test tests/skill-authoring-contract.test.cjs` -> 19/19 pass
- [x] `node --test tests/phase-28.5-baseline.test.cjs` -> 9/9 pass (no baseline re-lock needed)
- [x] `npm test` -> 2247 total / 2227 pass / 0 fail / 20 skipped, exit 0 (was 2195/32/20 pre-fix)
- [x] No edits to STATE.md, ROADMAP.md, NOTICE, or manifests (28.6-04 owns these)
- [x] No edits to `scripts/validate-skill-length.cjs` (D-05 invariant honored)

## Self-Check: PASSED

- File `test-fixture/baselines/phase-20/design-after.md` exists: FOUND
- File `test-fixture/baselines/phase-20/explore-after.md` exists: FOUND
- File `test-fixture/baselines/phase-20/utility-skills/health-after.md` exists: FOUND
- Commit `e4e81f1`: FOUND (`git log --oneline | grep e4e81f1`)
- Commit `09a087a`: FOUND (`git log --oneline | grep 09a087a`)
