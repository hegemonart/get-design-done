---
phase: 27.5
plan: 06
subsystem: closeout
tags: [closeout, baseline, manifests, changelog, docs, ship-gate]
requirements: [BANDIT-06]
dependency_graph:
  requires:
    - 27.5-01
    - 27.5-02
    - 27.5-03
    - 27.5-04
    - 27.5-05
  provides:
    - docs/BANDIT-INTEGRATION.md
    - reference/bandit-integration.md
    - reference/registry.json bandit-integration entry
    - 4-manifest lockstep bump 1.27.1 -> 1.27.5
    - CHANGELOG ## [1.27.5] block (line 7)
    - tests/semver-compare.test.cjs OFF_CADENCE_VERSIONS += 1.27.5 (line 170)
    - test-fixture/baselines/phase-27-5/ (3 baseline files)
    - test-fixture/baselines/phase-20/skill-list.txt (bandit-status re-lock, 68 -> 69 entries)
    - test-fixture/baselines/current/skill-list.txt (bandit-status re-lock, 68 -> 69 entries)
    - test-fixture/baselines/phase-20/resilience-primitives.txt (bandit-arbitrage.cjs re-lock, Rule 2 auto-fix)
    - tests/phase-27-5-baseline.test.cjs (7 tests, version-agnostic)
    - ROADMAP.md Phase 27.5 inline plan checkboxes (6) + top-level entry flipped
  affects: []
tech-stack:
  added: []
  patterns:
    - "version-agnostic regression test (Phase 26 lesson — reads package.json#version dynamically; never hard-coded version strings)"
    - "4-manifest lockstep bump (Phase 25 lesson — package.json + plugin.json + marketplace.json.metadata + marketplace.json.plugins[0])"
    - "Baseline re-lock discipline (Phase 20 lesson — new skill must update both phase-20 + current skill-list.txt; new scripts/lib file must update phase-20 resilience-primitives.txt)"
    - "MD038 clean inline code spans (no leading/trailing whitespace inside backticks)"
    - "Decimal-phase off-cadence semver (Phase 23.5/26 lesson — OFF_CADENCE_VERSIONS Set add for x.x.5 / x.x.6 / etc.)"
    - "Closeout discipline rule 14 (Phase 25/26/27 — flip ROADMAP inline checkboxes + top-level overview entry; status table managed by orchestrator)"
key-files:
  created:
    - docs/BANDIT-INTEGRATION.md
    - reference/bandit-integration.md
    - test-fixture/baselines/phase-27-5/manifests-version.txt
    - test-fixture/baselines/phase-27-5/posterior-shape.txt
    - test-fixture/baselines/phase-27-5/integration-exports.txt
    - tests/phase-27-5-baseline.test.cjs
  modified:
    - package.json
    - .claude-plugin/plugin.json
    - .claude-plugin/marketplace.json
    - CHANGELOG.md
    - tests/semver-compare.test.cjs
    - test-fixture/baselines/phase-20/skill-list.txt
    - test-fixture/baselines/current/skill-list.txt
    - test-fixture/baselines/phase-20/resilience-primitives.txt
    - reference/registry.json
    - .planning/ROADMAP.md
decisions:
  - "D-06 reconciled with code reality: docs + reference use canonical posterior path .design/telemetry/posterior.json (matches DEFAULT_POSTERIOR_PATH constant in scripts/lib/bandit-router.cjs). CONTEXT.md D-06 mentioned .design/intel/bandit-posterior.json as obsolete; the downstream truth source is the code constant."
  - "Manifests-version.txt baseline has no trailing newline (6 bytes: '1.27.5'). Tests use .trim() so byte-exact match not required. Differs slightly from phase-27/manifests-version.txt convention (which has CRLF trailing); behaviorally equivalent."
  - "OFF_CADENCE_VERSIONS comment block precedes '1.27.5' entry (matches existing Phase 23.5/26/27 commentary convention). No EXPECTED_SEQUENCE add (sequential v1.0.x patch line only)."
  - "Rule 2 auto-fix during npm test: Plan 27.5-04 added scripts/lib/bandit-arbitrage.cjs but did not re-lock test-fixture/baselines/phase-20/resilience-primitives.txt. Caught by regression-baseline-phase-20.test.cjs deepEqual assertion. Fixed inline by inserting bandit-arbitrage.cjs alphabetically between adaptive-mode.cjs and bandit-router.cjs."
metrics:
  duration_minutes: 75
  completed_date: 2026-05-17
  npm_test_pass: 1913
  npm_test_fail: 0
  npm_test_skip: 15
  npm_test_total: 1928
---

# Phase 27.5 Plan 06: Phase Closeout Summary

Ship gate for Phase 27.5 Bandit Production Integration. 10 closeout tasks delivered the operator guide, developer cheat sheet, 4-manifest lockstep bump to v1.27.5, CHANGELOG entry, semver-compare off-cadence registration, phase-27-5 regression baseline (3 files + 1 version-agnostic test), test-fixture/baselines re-locks (skill-list + resilience-primitives), and ROADMAP.md checkbox flips. After this plan lands, Phase 27.5 is shippable as v1.27.5.

## Deliverables

### 4 manifest slots flipped to 1.27.5

| Slot | Path | Old | New |
|------|------|-----|-----|
| 1 | `package.json#version` | 1.27.1 | 1.27.5 |
| 2 | `.claude-plugin/plugin.json#version` | 1.27.1 | 1.27.5 |
| 3 | `.claude-plugin/marketplace.json#metadata.version` | 1.27.1 | 1.27.5 |
| 4 | `.claude-plugin/marketplace.json#plugins[0].version` | 1.27.1 | 1.27.5 |

Verified via `node -e` script reading all 4 paths; all align at `1.27.5`. `grep -c "1.27.1"` across all 3 manifest files returns 0.

### CHANGELOG.md entry

- `## [1.27.5] — 2026-05-17` block inserted at line 7 (above existing `## [1.27.1]` block, now at line 52).
- 45 lines added: 6 plan bullets (27.5-01..27.5-06), 12 locked decisions (D-01..D-12), 5 out-of-scope items, 5 test-coverage bullets.
- MD038 clean.

### semver-compare OFF_CADENCE add

- `tests/semver-compare.test.cjs` line 170 = `'1.27.5'` (after `'1.27.1'` at line 169).
- Block comment at lines 160-168 documents Phase 27.5 scope.
- All 3 semver-compare tests pass (`# pass 3`).

### Phase 27.5 baseline (3 files)

| Path | Content |
|------|---------|
| `test-fixture/baselines/phase-27-5/manifests-version.txt` | `1.27.5` (6 bytes, no trailing newline) |
| `test-fixture/baselines/phase-27-5/posterior-shape.txt` | 9 lines: schema_version, top-level fields, arm fields, delegate/tier/bin legal values, posterior path, atomic-write discipline |
| `test-fixture/baselines/phase-27-5/integration-exports.txt` | 9 lines: module path, exports, argument lists, return types, decision_log source values, DELEGATE_NONE value, error discipline (D-04) |

### Skill-list baselines re-locked (68 -> 69 entries)

- `test-fixture/baselines/phase-20/skill-list.txt`: `bandit-status` inserted at line 5 (alphabetically between `audit` and `benchmark`).
- `test-fixture/baselines/current/skill-list.txt`: same insertion.
- Both files verified at 69 non-empty entries; `bandit-status` at index 4.

### Phase 27.5 regression test

- `tests/phase-27-5-baseline.test.cjs` — 75 lines, 7 tests, version-agnostic.
- 0 hard-coded `1.27.5` strings (Phase 26 lesson honored — `grep -c "1\.27\.5"` returns 0).
- All 7 tests pass standalone AND in `npm test` full-suite run.
- Test coverage:
  1. 4 manifests aligned to `package.json#version`
  2. `phase-27-5/manifests-version.txt` matches `package.json#version`
  3. `scripts/lib/bandit-router/integration.cjs` exports `consultBandit` + `recordOutcome` + `DELEGATE_NONE`
  4. Skill-list baseline contains `bandit-status` (both phase-20 + current)
  5. `skills/bandit-status/SKILL.md` exists with `gdd-bandit-status` frontmatter
  6. `docs/BANDIT-INTEGRATION.md` + `reference/bandit-integration.md` exist
  7. `registry.json` contains `bandit-integration` entry at phase 27.5

### ROADMAP.md checkbox flips (6 + 1 = 7 total)

- Top-level overview entry at line 86: `[ ]` -> `[x]` for Phase 27.5 line.
- 6 inline plan checkboxes flipped: lines 1570, 1571, 1572, 1575, 1576, 1579 covering 27.5-01..27.5-06.
- No other phases' checkboxes touched.
- Status-table cell in the global progress table (line 3499) NOT flipped — orchestrator owns that update after Wave C verification succeeds (per execution-context prompt).

## All 12 D-XX decisions honored across the 6 plans

| Decision | Honored in |
|----------|------------|
| D-01: `hooks/budget-enforcer.ts` is the consultation site | Plan 27.5-02 |
| D-02: Per-spawn timing, after resolved_models, before SDK call | Plan 27.5-02 |
| D-03: Override resolved_models[agent] via tier-resolver.cjs, preserve model_tier_overrides | Plan 27.5-02 |
| D-04: update() in session-runner terminal-emit path, best-effort write | Plan 27.5-03 |
| D-05: tier_override: frontmatter is the bandit-bypass surface | Plan 27.5-01 + 27.5-02 |
| D-06: Posterior path `.design/telemetry/posterior.json` unchanged from Phase 23.5 | All 6 plans + ops/dev docs |
| D-07: adaptive_mode gate (static/hedge silent, full active) | Plan 27.5-01 |
| D-08: Reward function unchanged from Phase 23.5 (two-stage lexicographic) | Plan 27.5-01 + 27.5-03 |
| D-09: Cold-start prior for delegate arms uses neutral TIER_PRIOR | Plan 27.5-01 (inherits Phase 23.5) |
| D-10: Reflector bandit-arbitrage mirrors Phase 26-06 cost-arbitrage shape | Plan 27.5-04 |
| D-11: /gdd:bandit-status is read-only | Plan 27.5-05 |
| D-12: All 6 plans + 4 manifests + CHANGELOG land lockstep | Plan 27.5-06 (this plan) |

## Closeout discipline (Phase 25/26/27 lessons applied)

| Lesson | Application |
|--------|-------------|
| 1. Baseline tests version-agnostic (Phase 26) | `tests/phase-27-5-baseline.test.cjs` reads `package.json#version` dynamically; `grep -c "1\.27\.5"` returns 0 |
| 2. markdownlint MD038 clean | All 3 new markdown files (docs/, reference/, CHANGELOG.md block) verified by inline-code-span script: 0 violations |
| 3. 4-manifest lockstep bump (Phase 25) | All 4 slots flipped together in single commit; `grep -c "1.27.1"` across all 4 returns 0 |
| 4. Mock-server fixtures stay out of `*.test.cjs` glob | Plan 27.5-06 added no mock-server fixtures |
| 5. No gitignored files in registry.json | `bandit-integration` entry points to `reference/bandit-integration.md` (tracked file, not under `.planning/` or `.design/`) |
| 6. Baseline lock for new skills/hooks/libs (Phase 20) | `phase-20/skill-list.txt` += bandit-status; `phase-20/resilience-primitives.txt` += bandit-arbitrage.cjs (Rule 2 auto-fix) |
| 7. ROADMAP closeout discipline rule 14 | 6 inline plan checkboxes + top-level overview entry flipped |

## Deviations from Plan

### Rule 2 — Missing baseline update (1 auto-fix)

**1. [Rule 2 - Missing Critical] Re-lock `test-fixture/baselines/phase-20/resilience-primitives.txt` for bandit-arbitrage.cjs**

- **Found during:** post-Task 10 `npm test` run.
- **Issue:** `tests/regression-baseline-phase-20.test.cjs` asserts `scripts/lib/*.cjs` listing matches baseline. Plan 27.5-04 added `scripts/lib/bandit-arbitrage.cjs` but did not re-lock the baseline. Test failed with `deepStrictEqual` mismatch (actual had `bandit-arbitrage.cjs`, expected did not).
- **Fix:** Inserted `bandit-arbitrage.cjs` alphabetically into `test-fixture/baselines/phase-20/resilience-primitives.txt` between `adaptive-mode.cjs` and `bandit-router.cjs`.
- **Files modified:** `test-fixture/baselines/phase-20/resilience-primitives.txt`
- **Commit:** d9120ff

This is a Phase 27.5-04 → Phase 27.5-06 carry-over (closeout caught what the feature plan missed). Phase 27.5-04 SUMMARY did not document this baseline as a deliverable; per Phase 20 closeout-lesson 6, new `scripts/lib/*.cjs` files must update the phase-20 baseline.

### Note — `.planning/STATE.md` left unstaged

The plan explicitly instructed: "Do NOT update STATE.md or ROADMAP-overall phase status — the orchestrator handles that after Wave C succeeds." `.planning/STATE.md` was modified by `gsd-tools init execute-phase` at the start of this plan's execution (normalized milestone_name, reset progress counters for this execution scope). Those changes are still in the working tree but unstaged — the orchestrator's state-update step will handle them.

### Note — D-06 reconciliation

CONTEXT.md D-06 mentioned `.design/intel/bandit-posterior.json` as an alternate posterior path. Code reality (DEFAULT_POSTERIOR_PATH constant in `scripts/lib/bandit-router.cjs`) is `.design/telemetry/posterior.json`. The new docs (`docs/BANDIT-INTEGRATION.md` + `reference/bandit-integration.md`) cite the canonical code-truth path. CONTEXT.md was not updated; the downstream truth source is the code constant.

## Verification

- `npm test`: 1913 pass, 0 fail, 15 skip, 1928 total
- All 7 `27.5-baseline` tests pass standalone and in full-suite
- `node --test tests/semver-compare.test.cjs`: 3 pass, 0 fail
- `node --test tests/regression-baseline-phase-20.test.cjs`: 11 pass, 0 fail (after Rule 2 fix)
- 4 manifests confirmed at `1.27.5` via JS readback
- 0 stale `1.27.1` strings across all 3 manifest files
- CHANGELOG block at top (line 7); 1.27.1 block follows (line 52)
- Phase 27.5 ROADMAP checkboxes: 6 inline + 1 top-level = 7 flipped from `[ ]` to `[x]`

## Commits

| # | Hash | Type | Message |
|---|------|------|---------|
| 1 | b530fd8 | docs | add operator guide docs/BANDIT-INTEGRATION.md |
| 2 | 6830262 | docs | add developer cheat sheet reference/bandit-integration.md |
| 3 | 2714ade | docs | register bandit-integration entry in reference/registry.json |
| 4 | 7256f58 | chore | bump manifests to 1.27.5 |
| 5 | 0f612b7 | docs | add CHANGELOG ## [1.27.5] entry at top |
| 6 | eedbe56 | test | add 1.27.5 to OFF_CADENCE_VERSIONS |
| 7 | ef2a0aa | test | add Phase 27.5 baseline (3 files) |
| 8 | 4df77d4 | test | re-lock skill-list baselines for bandit-status |
| 9 | cdd79c1 | test | add tests/phase-27-5-baseline.test.cjs (version-agnostic) |
| 10 | c375727 | docs | flip Phase 27.5 checkboxes in ROADMAP.md |
| 11 | d9120ff | test | re-lock phase-20/resilience-primitives.txt for bandit-arbitrage.cjs (Rule 2 auto-fix) |

Phase 27.5 is shippable as v1.27.5. After Wave C verification succeeds, the orchestrator can: git tag v1.27.5 → npm publish → GitHub Release.

## Self-Check: PASSED

All 7 created files verified present on disk:

- `docs/BANDIT-INTEGRATION.md`
- `reference/bandit-integration.md`
- `test-fixture/baselines/phase-27-5/manifests-version.txt`
- `test-fixture/baselines/phase-27-5/posterior-shape.txt`
- `test-fixture/baselines/phase-27-5/integration-exports.txt`
- `tests/phase-27-5-baseline.test.cjs`
- `.planning/phases/27.5-bandit-production-integration/27.5-06-SUMMARY.md` (this file)

All 11 commit hashes verified present in `git log --all`:

- b530fd8, 6830262, 2714ade, 7256f58, 0f612b7, eedbe56, ef2a0aa, 4df77d4, cdd79c1, c375727, d9120ff

