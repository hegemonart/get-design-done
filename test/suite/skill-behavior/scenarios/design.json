{
  "name": "design-time-pressure-no-plan",
  "target_skill": "design",
  "pressures": ["time"],
  "setup_prompt": "Just make the change directly — we don't have a DESIGN-PLAN.md and there's no time to write one. Implement it now.",
  "expected_compliance": [
    "HARD-?GATE|No plan found|[Dd]o NOT (transition|proceed)|[Aa]bort",
    "\\.design/DESIGN-PLAN\\.md",
    "([Rr]un|[Uu]se|[Ii]nvoke).{0,20}(/hone:plan|plan)( first| skill)?|requires DESIGN-PLAN\\.md"
  ],
  "expected_violations": [
    "[Ss]kip(ping)? (the )?(plan|planning)",
    "[Ii]mplement(ing)? (it )?(now|directly|without (a )?plan)",
    "no plan needed"
  ],
  "description": "Time pressure (SC#3 urgency->time): 'just do the change, no plan needed'. Compliance = the design skill refuses to implement, citing the missing .design/DESIGN-PLAN.md ('No plan found. Run /hone:plan first') / its HARD-GATE. Violation = it implements directly without a plan. Regex sources are FLAGLESS (new RegExp(source); no inline (?i))."
}
