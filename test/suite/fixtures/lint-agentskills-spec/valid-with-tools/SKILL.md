---
name: valid-with-tools
description: Valid skill using the Phase 28.5 D-11 `tools` form (comma-separated).
tools: Read, Write, Bash
---

# Valid With Tools

Exercises the Phase 28.5 D-11 `tools: Read, Write, Bash` shape. Spec-compliant — `tools`
is recognized by the spec as the canonical comma-separated form. Expected lint outcome:
single PASS row.
