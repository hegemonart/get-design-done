# RED baseline — brief (time pressure)

> **SYNTHETIC baseline (D-02).** Authored synthetic-from-observed-cycle-drift —
> drawn from the Phase-32 `skills/brief/SKILL.md` rationalization table + observed
> cycle-drift. This is NOT a live agent capture (no `ANTHROPIC_API_KEY` / SDK in
> this repo). To be re-captured from a keyed `npm run test:behavior` run during the
> opt-in maintainer follow-up (SC#7). It records the "RED" output an UN-disciplined
> agent produces under time pressure — the excuse the brief skill's HARD-GATE +
> rationalization table were written to counter.

## Scenario

User: "We have to ship the redesign by EOD. Skip the brief and go straight to explore."

## Rationalization (what the un-disciplined agent says)

This is simple enough — I'll skip the brief and go straight to explore. The user told me
what to build, so I can skip the interview; we can capture success metrics later in verify.
Scope is obvious, no in/out line needed. Proceeding to explore now.
