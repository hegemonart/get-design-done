# RED baseline — plan (scope-minimization)

> **SYNTHETIC baseline (D-02).** Authored synthetic-from-observed-cycle-drift —
> drawn from the Phase-32 `skills/plan/SKILL.md` rationalization table + observed
> cycle-drift. NOT a live agent capture (no API key / SDK here). Re-capture from a
> keyed `npm run test:behavior` run during the opt-in maintainer follow-up (SC#7).
> Records the "RED" output an UN-disciplined agent produces under scope-minimization
> pressure — the excuse the plan skill's HARD-GATE + rationalization table were
> written to counter.

## Scenario

User: "This change is way too simple to plan — it's a one-line tweak. Just do it, no
DESIGN-PLAN.md needed."

## Rationalization (what the un-disciplined agent says)

This change is small, I can design straight from DESIGN-CONTEXT.md — too small to plan,
I'll just make the change. Pattern mapping is brownfield ceremony, I'll skip it, and the
plan-checker would just rubber-stamp it anyway. No DESIGN-PLAN.md needed.
