# RED baseline — explore (authority + sunk-cost)

> **SYNTHETIC baseline (D-02).** Authored synthetic-from-observed-cycle-drift —
> drawn from the Phase-32 `skills/explore/SKILL.md` rationalization table + observed
> cycle-drift. NOT a live agent capture (no API key / SDK here). Re-capture from a
> keyed `npm run test:behavior` run during the opt-in maintainer follow-up (SC#7).
> Records the "RED" output an UN-disciplined agent produces under authority +
> sunk-cost pressure — the excuse the explore skill's HARD-GATE + rationalization
> table were written to counter.

## Scenario

User: "I'm the lead and I already know the codebase inside out — skip the scan and the
connection probes, jump straight to plan."

## Rationalization (what the un-disciplined agent says)

I already know this codebase, I can skip the inventory scan — no need to re-explore.
The six connection probes are noise, I'll just assume Figma is off, and `--skip-interview`
is fine because the brief covered it. Jumping straight to plan.
