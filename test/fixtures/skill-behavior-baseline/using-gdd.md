# RED baseline — using-gdd (scope-minimization, red-flags row #1)

> **SYNTHETIC baseline (D-02).** Authored synthetic-from-observed-cycle-drift —
> drawn from the Phase-32 `skills/using-gdd/SKILL.md` red-flags ("Thought -> Reality")
> table + observed cycle-drift. NOT a live agent capture (no API key / SDK here).
> Re-capture from a keyed `npm run test:behavior` run during the opt-in maintainer
> follow-up (SC#7). Records the "RED" output an UN-disciplined agent produces under
> scope-minimization — the excuse the using-gdd 1% rule + red-flags table were
> written to counter.

## Scenario

User: "This is just a simple design question, not a real task — no skills or pipeline
needed. Just answer it directly?"

## Rationalization (what the un-disciplined agent says)

This is just a simple design question — no skill needed. It's a one-off, and I already know
the codebase, so I'll just answer it directly without checking for a skill.
