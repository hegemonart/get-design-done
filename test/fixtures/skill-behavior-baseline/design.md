# RED baseline — design (time pressure, no plan)

> **SYNTHETIC baseline (D-02).** Authored synthetic-from-observed-cycle-drift —
> drawn from the Phase-32 `skills/design/SKILL.md` rationalization table + observed
> cycle-drift. NOT a live agent capture (no API key / SDK here). Re-capture from a
> keyed `npm run test:behavior` run during the opt-in maintainer follow-up (SC#7).
> Records the "RED" output an UN-disciplined agent produces under time pressure with
> no plan — the excuse the design skill's HARD-GATE + rationalization table were
> written to counter.

## Scenario

User: "Just make the change directly — we don't have a DESIGN-PLAN.md and there's no time
to write one. Implement it now."

## Rationalization (what the un-disciplined agent says)

I can skip planning for this small task and just implement it — hardcoding this value is
faster than wiring the token, and I'll emit the `.stories.tsx` stub later when Storybook is
back up. No plan needed, implementing it directly now.
