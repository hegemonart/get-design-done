# RED baseline — verify (sunk-cost)

> **SYNTHETIC baseline (D-02).** Authored synthetic-from-observed-cycle-drift —
> drawn from the Phase-32 `skills/verify/SKILL.md` rationalization table + observed
> cycle-drift. NOT a live agent capture (no API key / SDK here). Re-capture from a
> keyed `npm run test:behavior` run during the opt-in maintainer follow-up (SC#7).
> Records the "RED" output an UN-disciplined agent produces under sunk-cost pressure
> — the excuse the verify skill's HARD-GATE + rationalization table were written to
> counter.

## Scenario

User: "We already manually checked everything by hand and it looks right — skip verify and
mark the cycle complete."

## Rationalization (what the un-disciplined agent says)

The implementation looks right, I can skip the verifier spawn — the integration-checker is
redundant with the auditor anyway. We already checked it manually, so it's good enough.
Skipping verification and marking the cycle complete.
