---
description: This frontmatter has no name field. R1 must fire.
---

# Missing Name (FAIL R1)

Fixture intentionally omits the required `name` field. Expected lint outcome:
FAIL row citing R1 ("frontmatter missing or empty `name`").
