---
name: valid-minimal
description: A minimal valid skill exercising only the two REQUIRED spec fields.
---

# Valid Minimal

Body content under 500 lines. This fixture covers the bare-minimum agentskills.io spec
contract: a `name` slug + a `description`. No optional fields. Expected lint outcome:
single PASS row.
