---
name: valid-with-allowed-tools
description: Valid skill using the spec's `allowed-tools` form (space-separated).
allowed-tools: Read Write Bash
---

# Valid With Allowed Tools

Exercises the agentskills.io spec's `allowed-tools: Read Write Bash` shape (Experimental
flag, but recognized). Spec-compliant. Expected lint outcome: single PASS row.
